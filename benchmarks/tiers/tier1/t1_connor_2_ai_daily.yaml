# Tier 1 — Connor 真实场景域：AI 领域日报
# F-08 patch：按用户拍板内容填入（2026-05-28）
# domain: connor_real_world

task_id: T1-CONNOR-2
tier: 1
domain: connor_real_world
prompt: "出一份近 24h 的 AI 领域日报，5-10 条新闻，每条带 1 句话评论。格式：Markdown 列表，含标题 / 来源 / 1 句话点评。"
expected_events:
  - event_type: TOOL_CALL_STARTED
    required_fields: {}  # web.search 或 mcp.perplexity 工具调用（至少 1 次）
  - event_type: TOOL_CALL_COMPLETED
    required_fields: {}  # 工具调用完成
  - event_type: MODEL_CALL_COMPLETED
    required_fields: {}  # 生成日报内容
timeout_seconds: 300
partial_signals:
  - event_type: TOOL_CALL_STARTED   # 至少发起 web 搜索
  - event_type: TOOL_CALL_COMPLETED  # 搜索完成
notes: >
  scorer 宽松策略：允许 web.search 任意工具（不限于 perplexity）≥ 1 次。
  原因：T1-CONNOR-2/3 依赖实时 web，perplexity MCP 可能 quota fail → 系统可降级到其他 search 工具。
  PASS 条件：产出 Markdown 含 ≥ 5 条新闻 + TOOL_CALL_STARTED 事件（任何 web 搜索工具）。
  无 mock 数据文件（依赖实时 web）。
rubric_id: tier1-v1
