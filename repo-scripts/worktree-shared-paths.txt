# 这些路径会从主仓库共享到各个 linked worktree。
# 每行一个相对仓库根目录的路径，支持空行与 # 注释。

AGENTS.local.md
CLAUDE.local.md
_references
_research
.specify/project-context.md
.specify/project-context.yaml
octoagent/.env
octoagent/.python-version
octoagent/.venv
octoagent/packages/provider/.env

# Claude Code 本地配置（hooks / permissions / pre-commit gate 等）。
# git-untracked 但每个 worktree 都需要——之前漏挂导致新建 worktree 时
# Claude Code 状态不完整。
# 注：``.agent-config/shared.md`` 与 ``.agent-config/templates`` 已是 git
# tracked，新 worktree 通过 git checkout 自然带上，无需软链。
.claude/settings.local.json

# 硬规则：git-tracked（或计划入库）的路径禁止加入本清单，只放 untracked 路径。
# 原因（2026-07 Codex review 实证，曾试图加 .claude/skills 被否）：
#   1. 旧 worktree 先拿到 untracked 软链，之后 checkout/rebase 到真含该目录的
#      基线时，git 会报 "untracked working tree files would be overwritten" 中止；
#   2. --force / create-worktree.sh --force-sync 会 rm -rf 掉 tracked 真实目录
#      换成软链，把工作树弄成 D + ?? 的脏状态。
# project 级 Claude Code skill（.claude/skills/ 等）一律走 git 入库单源分发。

# 可选：如果你希望所有 worktree 共用前端依赖，再取消这一行注释。
# octoagent/frontend/node_modules
