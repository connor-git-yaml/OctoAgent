# Canonical Spec Redirect

本仓库当前使用 `.specify/features/<feature-name>/` 作为 Feature 制品根目录。  
Feature 044 的正式规范位于：

- `.specify/features/044-settings-center-refresh/spec.md`

本文件仅保留为脚本兼容占位，避免重复维护多份 spec。
