# OctoAgent 通用配置示例
# 复制为 .env 并根据需要调整
#
# 对齐 contracts/deployment.md SS3.1

# ============================================================
# LLM 运行模式
# ============================================================
# litellm: 通过 LiteLLM Proxy 调用真实 LLM（需要先启动 Proxy）
# echo: Echo 模式，返回输入回声（开发/测试用，无需 Proxy）
OCTOAGENT_LLM_MODE=litellm

# LLM 调用超时（秒）
OCTOAGENT_LLM_TIMEOUT_S=30

# ============================================================
# LiteLLM Proxy 连接配置（OctoAgent 应用侧）
# ============================================================
# Proxy 地址（与 docker-compose.litellm.yml 的端口映射一致）
LITELLM_PROXY_URL=http://localhost:4000

# Proxy 访问密钥（与 LITELLM_MASTER_KEY 一致）
# ⚠️ 必须替换为自定义密钥，不要使用示例值
LITELLM_PROXY_KEY=changeme-proxy-key

# ============================================================
# LiteLLM Proxy 管理密钥（Proxy 容器侧）
# ============================================================
# 此密钥用于 Docker Compose 中设置 Proxy 的 master key
# ⚠️ 必须替换为自定义密钥，不要使用示例值
LITELLM_MASTER_KEY=changeme-master-key

# ============================================================
# LiteLLM Proxy 端口映射（可选）
# ============================================================
# LITELLM_PORT=4000
