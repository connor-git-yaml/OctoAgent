# LLM Provider API Keys -- 仅注入到 LiteLLM Proxy 容器
# 复制为 .env.litellm 并填入真实 API Keys
#
# 安全注意: 此文件的实际副本 (.env.litellm) 必须在 .gitignore 中排除
# 对齐 contracts/deployment.md SS3.2 + Constitution C5（Least Privilege）

# ============================================================
# OpenAI（main + cheap 运行时 group 使用）
# ============================================================
# 获取: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-key-here

# ============================================================
# Anthropic（fallback 运行时 group 使用）
# ============================================================
# 获取: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key-here

# ============================================================
# 其他 Provider（按需添加）
# ============================================================
# GOOGLE_API_KEY=your-google-key-here
# AZURE_API_KEY=your-azure-key-here
