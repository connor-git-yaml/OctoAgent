# F087 fixture 严格 gitignore（R9 缓解）
#
# 任何非 .template 后缀文件 / 真实凭证文件都不进 git

# 排除：.real / 真实凭证 / .env / auth-profiles / api-key 文件
*.real
*.local
auth-profiles*.json
*.env
.env.*
mcp-servers.json
*api_key*
*API_KEY*
*token*
*TOKEN*
*secret*
*SECRET*

# 允许：.template / 脱敏文件（白名单优先）
!*.template
!.gitignore
!README.md
