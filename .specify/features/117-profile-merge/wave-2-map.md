# F117 Wave 2 — 详尽变更地图（Understand workflow 7-agent 产出）

> 6 并行 mapper + 完整性 critic（read-only）。下方是逐维度变更清单 + critic 抓出的 GAP/HAZARD。

> 实施按 critic 的安全子波序：2a 加性地基 / 2b+2c 耦合写+读切换 / wire 留 Wave 3 / works+dedup 留 Wave 4。


## 维度：runtime READ-path switch: capability_pack.resolve_worker_binding / resolve_worker_type_for_profile + chat.py _resolve_profile_model_alias / _resolve_owner_turn_executor_kind switch from reading worker_profiles to reading the unified agent_profiles(kind=worker) row

- **apps/gateway/src/octoagent/gateway/services/capability_pack.py** `resolve_worker_binding, lines 410-448 (the worker_profiles-first / agent_profiles-fallback 2-branch block, inside the `if normalized_profile_id:` after the builtin_worker_type check)` [high]
  - 现状：After the builtin singleton check, two sequential branches: (1) lines 410-430 reads `stored_profile = await self._stores.agent_context_store.get_worker_profile(normalized_profile_id)`; if not None AND `stored_profile.status != WorkerProfileStatus.ARCHIVED`, returns a _ResolvedWorkerBinding with worker_type='general', model_alias=stored_profile.model_alias or 'main', tool_profile=stored_profile.tool_profile or builtin('general').default_tool_profile, default_tool_groups=stored_profile.default_tool_groups or builtin, selected_tools=list(stored_profile.selected_tools), profile_revision=stored_profile.active_revision or draft_revision or 1, source_kind='worker_profile', profile_name=stored_profile.name. (2) lines 431-448 fallback reads `agent_profile = await self._stores.agent_context_store.get_agent_profile(normalized_profile_id)`; if not None returns binding with worker_type=fallback_worker_type, profile_revision=agent_profile.version, default_tool_groups=builtin(fallback).default_tool_groups (NOT from profile), selected_tools=[], source_kind='agent_profile', profile_name=agent_profile.name. Note the two branches use DIFFERENT field sources for tool fields (worker branch maps the 9 worker fields; agent branch ignores them and uses builtin defaults).
  - 目标：Collapse to a SINGLE branch reading the unified row: `agent_profile = await self._stores.agent_context_store.get_agent_profile(normalized_profile_id)`. Then import `is_worker_behavior_profile` (from octoagent.gateway.services.agent_decision) and branch on it: IF `agent_profile is not None and is_worker_behavior_profile(agent_profile) and agent_profile.status != WorkerProfileStatus.ARCHIVED` -> return the WORKER-shaped binding mapping the now-unified 9 fields: model_alias=agent_profile.model_alias or 'main', tool_profile=agent_profile.tool_profile or builtin('general').default_tool_profile, default_tool_groups=list(agent_profile.default_tool_groups or builtin('general').default_tool_groups), selected_tools=list(agent_profile.selected_tools), profile_revision=(agent_profile.active_revision or agent_profile.draft_revision or 1), worker_type='general', source_kind='worker_profile' (KEEP this literal — it is the behavior-identity of a worker binding and is consumed downstream; do NOT rename to 'agent_profile'), profile_name=agent_profile.name. ELIF `agent_profile is not None` (non-worker main/subagent agent profile, status not worker-meaningful) -> return the legacy agent-fallback binding UNCHANGED: worker_type=fallback_worker_type, model_alias=agent_profile.model_alias or 'main', tool_profile=agent_profile.tool_profile or builtin(fallback).default_tool_profile, default_tool_groups=list(builtin(fallback).default_tool_groups), selected_tools=[], profile_revision=agent_profile.version, source_kind='agent_profile', profile_name=agent_profile.name. This preserves BOTH prior code paths' exact field-mapping semantics while reading a single store. builtin_profile for the worker branch = self.get_worker_profile('general'); for the agent branch = self.get_worker_profile(fallback_worker_type).
  - 注：CRITICAL ORDERING: the worker branch must use the worker-field mapping (the 9 fields) and the non-worker branch must use builtin defaults — these are NOT interchangeable. Before Wave 1, a worker mirror row had empty default_tool_groups/selected_tools and would have produced wrong tool resolution if read via the old agent_profile fallback; Wave 1 populated them so the worker branch is now correct. `WorkerProfileStatus` is already imported (capability_pack.py:29). is_worker_behavior_profile is NOT yet imported into capability_pack — must add import.
- **apps/gateway/src/octoagent/gateway/services/capability_pack.py** `resolve_worker_type_for_profile, lines 462-472` [medium]
  - 现状：Reads `stored_profile = await self._stores.agent_context_store.get_worker_profile(normalized)` (line 469); returns None if stored_profile is None OR stored_profile.status == WorkerProfileStatus.ARCHIVED (line 470-471); else returns 'general'. Builtin singleton short-circuit at 466-468 stays.
  - 目标：Switch the store read to the unified row: `agent_profile = await self._stores.agent_context_store.get_agent_profile(normalized)`. Return None if `agent_profile is None OR NOT is_worker_behavior_profile(agent_profile) OR agent_profile.status == WorkerProfileStatus.ARCHIVED`; else return 'general'. The added `not is_worker_behavior_profile(agent_profile)` guard is REQUIRED — previously get_worker_profile only ever returned worker rows, so a non-worker agent profile id must now be excluded to preserve the 'returns worker_type only for worker profiles' contract.
  - 注：Keep builtin singleton short-circuit (466-468) unchanged.
- **apps/gateway/src/octoagent/gateway/routes/chat.py** `_resolve_profile_model_alias, lines 251-264` [low]
  - 现状：2-branch: line 256 `worker_profile = await store_group.agent_context_store.get_worker_profile(resolved_profile_id)`; if not None return worker_profile.model_alias. Lines 260-262 fallback `agent_profile = await store_group.agent_context_store.get_agent_profile(...)`; if not None return agent_profile.model_alias. Both branches read the SAME model_alias field, so behavior is identical regardless of which store the profile lives in.
  - 目标：Collapse to single read: `agent_profile = await store_group.agent_context_store.get_agent_profile(resolved_profile_id)`; if agent_profile is not None return `str(agent_profile.model_alias or '').strip()`; else return ''. No worker-detection needed here — model_alias is a shared field on the unified row and the worker-mirror row carries the worker's model_alias (Wave 1 copies model_alias=worker_profile.model_alias or 'main'). Drop the get_worker_profile call entirely.
  - 注：Lowest-risk of the four: model_alias is value-identical across the merged row. Worker mirror's model_alias is populated by both mirror builders (agent_context_entity_ensure.py:982 model_alias=worker_profile.model_alias or 'main'; worker_profile_ops.py:137 model_alias=profile.model_alias).
- **apps/gateway/src/octoagent/gateway/routes/chat.py** `_resolve_owner_turn_executor_kind, lines 267-276` [high]
  - 现状：Reads `worker_profile = await store_group.agent_context_store.get_worker_profile(resolved_profile_id)` (line 271); returns TurnExecutorKind.WORKER if found, else TurnExecutorKind.SELF. The presence of a row in worker_profiles IS the worker-detection signal here.
  - 目标：Switch to unified read with explicit worker detection: `agent_profile = await store_group.agent_context_store.get_agent_profile(resolved_profile_id)`; return TurnExecutorKind.WORKER if `agent_profile is not None and is_worker_behavior_profile(agent_profile)`, else TurnExecutorKind.SELF. The is_worker_behavior_profile guard is ESSENTIAL — get_worker_profile previously returned ONLY worker rows (presence==worker), but get_agent_profile now also returns main/subagent profiles which must map to SELF, not WORKER. Must import is_worker_behavior_profile into chat.py.
  - 注：Highest-semantics-risk of the chat.py pair: dropping the is_worker_behavior_profile guard would make every main agent profile resolve to TurnExecutorKind.WORKER, flipping the owner turn-executor decision. is_worker_behavior_profile lives in octoagent.gateway.services.agent_decision.
- **apps/gateway/src/octoagent/gateway/routes/chat.py** `_resolve_anchored_owner_profile_id (the other get_worker_profile reader), line 245` [medium]
  - 现状：Line 245 `if await store_group.agent_context_store.get_worker_profile(legacy_owner): continue` — uses get_worker_profile presence as a 'this legacy owner is a worker, skip it' filter when resolving the anchored owner profile id from control metadata.
  - 目标：NOT strictly in the named scope of this dimension's two helpers, but it is the SAME read-switch pattern in the same file and MUST be switched in Wave 2 to avoid a residual worker_profiles read: change to `agent_profile = await store_group.agent_context_store.get_agent_profile(legacy_owner)` and `if agent_profile is not None and is_worker_behavior_profile(agent_profile): continue`. Flag for inclusion — leaving it as get_worker_profile would mean Wave 4 (deleting worker_profiles tables) breaks this branch. Confirm with main session whether to fold into this dimension or the authoring-write dimension.
  - 注：Behavior subtlety: old code skipped ANY id present in worker_profiles (including archived). New code with is_worker_behavior_profile skips any worker-kind agent row (archived or not, since this is a 'is it a worker' identity check, not a liveness check). Equivalent for the 'is this a legacy direct-owner worker' intent. Reuses same is_worker_behavior_profile import added above.
  - **gotchas**：ARCHIVE-STATUS HAZARD (confirmed concrete): worker_service.py:848 `_handle_worker_profile_archive` writes status=ARCHIVED via `save_worker_profile(existing.model_copy(...))` (line 861) ONLY to worker_profiles, and does NOT call `_sync_worker_profile_agent_profile`. The three sync callers are draft/publish/apply (worker_service.py:948, 1031, 1095) — archive is NOT among them. So on a live DB, after archiving a worker, worker_profiles.status='archived' but the agent_profiles mirror row keeps its prior status (e.g. 'active'). CONSEQUENCE: if resolve_worker_binding/resolve_worker_type_for_profile switch to reading agent_profile.status != ARCHIVED, an archived worker would STILL resolve as bindable (stale mirror). The read-switch CANNOT be landed safely until the archive path is made to sync the mirror status (the KEY FACT's 'Wave 2 read-switch must close this'). Required Wave 2 companion change (write-side, separate dimension): _handle_worker_profile_archive must also save the agent_profiles row with status=ARCHIVED + archived_at — either by calling _sync_worker_profile_agent_profile after archiving, or by directly updating the unified row once authoring writes to agent_profiles. Until that lands, the agent_profile.status check is a behavior REGRESSION risk vs the current worker_profiles.status check. / WORKER DETECTION MUST USE is_worker_behavior_profile, NOT a kind=='worker' literal: agent_decision.py:112 is_worker_behavior_profile does dual judgment — `agent_profile.kind == 'worker'` OR metadata source_kind=='worker_profile_mirror'/source_worker_profile_id present. KEY FACT: live instances have NO 'kind' column on agent_profiles; _row_to_agent_profile (agent_context_store.py:1380) defaults kind='main' when the column is absent (try/KeyError guard). So on a live DB a worker mirror row hydrates with kind='main' and is detected ONLY via the metadata fallback. Any new code that checks `agent_profile.kind == 'worker'` directly instead of is_worker_behavior_profile will mis-classify every worker on un-migrated instances as non-worker. / STATUS COLUMN-ABSENT DEFAULT: _row_to_agent_profile (agent_context_store.py:~1395) defaults status to WorkerProfileStatus.ACTIVE when the 'status' column is absent (un-migrated DB). So on un-migrated instances every agent_profiles row reads status=ACTIVE regardless of the real worker status — combined with the archive hazard above, an archived worker reads as ACTIVE via the unified row on un-migrated DBs. The read path's `status != ARCHIVED` check therefore silently degrades to 'never archived' on un-migrated instances. This is acceptable ONLY if the migration is guaranteed applied before the read-switch is exercised; otherwise it is a real behavior gap. Flag to main session: is migration_117 apply a hard prerequisite for the read-switch, or must the read path also consult source metadata for archived state? / profile_revision FIELD SOURCE differs between branches and must be preserved: the worker branch must compute revision as `active_revision or draft_revision or 1` (the worker-field semantics, now on the unified row), while the non-worker agent branch uses `agent_profile.version`. The Wave 1 mirror builders set version=max(active_revision or draft_revision, 1), so for a worker row active_revision/draft_revision and version are aligned — but to preserve EXACT current behavior keep the worker branch reading active_revision/draft_revision (matching old worker_profiles branch) not version. / DEFAULT tool_profile DIVERGENCE: WorkerProfile model default tool_profile='minimal' (agent_context.py:247) vs AgentProfile default tool_profile='standard' (agent_context.py:211). The Wave 1 mirror builders copy tool_profile=worker_profile.tool_profile (agent_context_entity_ensure.py:983 uses `or 'standard'`; worker_profile_ops.py:138 uses raw profile.tool_profile). For an EXISTING worker mirror this is already populated, so the read uses the copied value with `or builtin.default_tool_profile` fallback — equivalent to current. Do not introduce a new 'standard' default in the read path; keep the `or builtin('general').default_tool_profile` fallback exactly as the old worker branch had it. / source_kind='worker_profile' LITERAL IS LOAD-BEARING — do NOT rename to 'agent_profile' in the worker branch. _ResolvedWorkerBinding.source_kind distinguishes worker_profile vs agent_profile vs builtin_singleton/builtin_fallback downstream; the unified-read worker branch must keep returning source_kind='worker_profile' to preserve the binding identity, even though it now physically reads from agent_profiles. The non-worker branch keeps source_kind='agent_profile'. / EXHAUSTIVE worker_profiles READERS in the two target files: capability_pack.py has THREE get_worker_profile store reads to switch — line 410 (resolve_worker_binding), line 469 (resolve_worker_type_for_profile), line 1207 (capability_provider_selection resolver, ALREADY agent_profile-first with worker fallback at 1201/1207 — that one can drop the worker fallback in Wave 2 but verify it is in scope). chat.py has THREE get_worker_profile reads — line 245 (_resolve_anchored_owner_profile_id), line 256 (_resolve_profile_model_alias), line 271 (_resolve_owner_turn_executor_kind). The dimension prompt named only the helpers at 256/271; lines 245 (chat) and 410/469/1207 (capability_pack) are the complete set and all must be covered to avoid residual worker_profiles reads before Wave 4 deletes the table. / IMPORT ADDITIONS NEEDED: capability_pack.py must add `from .agent_decision import is_worker_behavior_profile` (or the fully-qualified octoagent.gateway.services.agent_decision path); chat.py must add the same import. WorkerProfileStatus is already imported in capability_pack.py:29 but verify chat.py imports it (chat.py currently does not reference WorkerProfileStatus — only TurnExecutorKind — so if the chat read path needs an ARCHIVED check it must add that import; per current chat.py logic neither helper checks status, so chat.py likely needs only is_worker_behavior_profile).

## 维度：authoring domain file #1 — apps/gateway/src/octoagent/gateway/services/control_plane/worker_profile_ops.py (93 occurrences). Wave 2 must (a) switch the authoring write path from worker_profiles/worker_profile_revisions tables to the unified agent_profiles(kind=worker) + new agent_profile_revisions table via AgentProfile objects, (b) delete the mirror builder, (c) execute the rename pass (WorkerProfile* classes → AgentProfile*, enums, action_ids). Every method is mapped below with exact file:line and the target rewrite.

- **apps/gateway/src/octoagent/gateway/services/control_plane/worker_profile_ops.py** `imports block, lines 27-39` [low]
  - 现状：Imports AgentProfile, AgentProfileScope, ControlPlaneCapability, ControlPlaneSupportStatus, DynamicToolSelection, Work, WorkerProfile, WorkerProfileDynamicContext, WorkerProfileOriginKind, WorkerProfileRevision, WorkerProfileStatus from octoagent.core.models.
  - 目标：After Wave 2 rename: WorkerProfile -> (DELETE — no longer constructed; authoring builds AgentProfile directly); WorkerProfileRevision -> AgentProfileRevision (new model added in Wave 2, mirrors WorkerProfileRevision shape exactly: revision_id/profile_id/revision/change_summary/snapshot_payload/created_by/created_at); WorkerProfileOriginKind -> AgentProfileOriginKind; WorkerProfileStatus -> AgentProfileStatus. KEEP: AgentProfile, AgentProfileScope, ControlPlaneCapability, ControlPlaneSupportStatus, DynamicToolSelection, Work, WorkerProfileDynamicContext (the dynamic-context VIEW class is NOT in Wave-2 rename scope per prompt — it's a runtime UI view, not the profile model). Note: enums are currently aliases of the same StrEnum values (status/origin_kind values unchanged: draft/active/archived, builtin/custom/cloned/extracted) so AgentProfile field types (status: WorkerProfileStatus, origin_kind: WorkerProfileOriginKind at agent_context.py:228-229) must be renamed in the model file in lockstep — out of THIS file's scope but a hard cross-file dependency.
  - 注：Pure import/symbol rename except dropping WorkerProfile. The model-file enum rename (agent_context.py:80,86) is a separate file but THIS file's type annotations reference them, so the two must land in the same wave commit or imports break.
- **apps/gateway/src/octoagent/gateway/services/control_plane/worker_profile_ops.py** `_build_agent_profile_from_worker_profile, lines 109-155` [high]
  - 现状：The MIRROR BUILDER. Takes a WorkerProfile + revision + optional existing AgentProfile; copies WorkerProfile fields into a fresh AgentProfile(kind='worker'); stamps metadata with source_kind='worker_profile_mirror', behavior_agent_slug, worker_profile_id, worker_profile_revision, worker_profile_status. Wave 1 already populates the 9 absorbed fields (summary, default_tool_groups, selected_tools, runtime_kinds, status, origin_kind, draft_revision, active_revision, archived_at) into the unified row.
  - 目标：DELETE this method entirely. Rationale: once authoring writes AgentProfile(kind='worker') directly to agent_profiles (no separate worker_profiles row), there is no source WorkerProfile to mirror FROM — the unified row IS the authored object. Its only callers are _sync_worker_profile_agent_profile (line 164, also being reworked) and worker_service.py:152 (_build_agent_profile_mirror at clone/spawn — that caller must be rewritten to read/construct the unified AgentProfile directly, separate file). CRITICAL — preserve the metadata stamping on the new write path: is_worker_behavior_profile (agent_decision.py:112-125) does DUAL judgment — kind=='worker' OR metadata source_kind=='worker_profile_mirror'. On live instances agent_profiles has NO kind column, so worker detection falls back to metadata ONLY. Therefore the new _save_worker_profile_draft write path MUST keep setting metadata['source_kind']='worker_profile_mirror' + behavior_agent_slug (normalize_behavior_agent_slug(name or profile_id)) on every authored worker row, or live-instance worker detection silently breaks. Also preserve materialize_agent_behavior_files(is_worker_profile=True) side-effect (currently in _sync, line 171-177).
  - 注：Deletion is correct but the metadata-stamping responsibility it carried is load-bearing for worker-detection on schema-less live instances. Must migrate that stamping into the draft-save path, not just drop it.
- **apps/gateway/src/octoagent/gateway/services/control_plane/worker_profile_ops.py** `_sync_worker_profile_agent_profile, lines 157-178` [high]
  - 现状：Async. Reads existing AgentProfile via get_agent_profile(profile.profile_id), builds the mirror via _build_agent_profile_from_worker_profile, saves it via save_agent_profile, then materializes agent-private behavior files (resolve_behavior_agent_slug + materialize_agent_behavior_files is_worker_profile=True). Called from worker_service.py:948, 1031, 1095 (publish + restore paths) to push the worker_profiles row into the agent_profiles mirror after each publish/restore.
  - 目标：DELETE the mirror-sync semantics. Once _save_worker_profile_draft and _publish_worker_profile_revision write the unified agent_profiles row directly, there is no second row to sync — the save IS the canonical write. The behavior-file materialization side-effect (resolve_behavior_agent_slug + materialize_agent_behavior_files is_worker_profile=True, lines 171-177) must NOT be lost: fold it into the draft/publish write path (so authored worker rows still get their agent-private behavior dir). The three callers in worker_service.py (948,1031,1095) drop their _sync_worker_profile_agent_profile calls. NOTE the archive-path bug (KEY FACT b): _handle_worker_profile_archive (worker_service.py:855-870) currently saves worker_profiles WITHOUT calling _sync — leaving the mirror status stale. After Wave 2 the archive path writes the unified row directly via save_worker_profile→(renamed)save_agent_profile path, so status='archived' is authoritative with no second row to go stale — this CLOSES that bug structurally.
  - 注：The materialization side-effect is the salvage target. Verify the behavior dir for archived/draft profiles is still created on the consolidated write path.
- **apps/gateway/src/octoagent/gateway/services/control_plane/worker_profile_ops.py** `_worker_profile_label (76-84), _worker_profile_summary (86-90)` [low]
  - 现状：Static helpers. _worker_profile_label maps worker_type->display label; _worker_profile_summary builds a Chinese summary string from capabilities+tool_groups. No DB / WorkerProfile object access.
  - 目标：NO functional change. Method names contain 'worker_profile' — optional cosmetic rename only if the rename pass standardizes method names; NOT required by the read/write switch. Recommend KEEP names (these describe worker archetype labels, semantically worker-specific). Flag for the rename-pass owner: these are method-local names, zero external callers beyond worker_service + _resolve_builtin_worker_source.
  - 注：Out of the structural switch; rename optional.
- **apps/gateway/src/octoagent/gateway/services/control_plane/worker_profile_ops.py** `_worker_snapshot_id, lines 92-95` [low]
  - 现状：Static. Returns f'worker-snapshot:{profile_id}:{revision}'. Used as revision_id (line 843) AND consumed as Work.effective_worker_snapshot_id elsewhere (worker_service.py:185,269,444,1157; delegation_plane.py).
  - 目标：NO change to the returned string format. The 'worker-snapshot:' prefix is a runtime VALUE format (delegation snapshot id + revision_id), NOT a type/action/route name — explicitly OUT of Wave-2 rename scope (renaming it would break stored revision_id values + Work.effective_worker_snapshot_id audit data, violating Durability). KEEP method + format verbatim.
  - 注：Tempting to rename but it is load-bearing persisted data; the prompt's rename scope is action_id/view-class/enum/route only. Leave untouched.
- **apps/gateway/src/octoagent/gateway/services/control_plane/worker_profile_ops.py** `_tool_selection_from_work (97-107), _build_worker_dynamic_context (202-265)` [low]
  - 现状：Build WorkerProfileDynamicContext (the runtime UI view) from a list of Work objects. No profile-table access. Returns WorkerProfileDynamicContext.
  - 目标：NO functional change. WorkerProfileDynamicContext is a runtime/UI VIEW class explicitly EXCLUDED from Wave-2 rename per prompt scope (Wave 2 renames the WorkerProfileStatus/OriginKind enums + view classes WorkerProfile* that mirror the model; the dynamic-context view is a distinct runtime aggregate, not the profile model). KEEP as-is. If the rename pass later decides view classes WorkerProfileDynamicContext->AgentProfileDynamicContext, that is a separate coordinated change touching the model file + FE; do NOT couple it to the read/write switch.
  - 注：Confirm with rename-pass owner whether WorkerProfileDynamicContext is in 'view classes' rename set. Prompt lists 'view classes WorkerProfile*->AgentProfile*' generically — needs explicit include/exclude decision. Flagged as a gotcha.
- **apps/gateway/src/octoagent/gateway/services/control_plane/worker_profile_ops.py** `_bind_worker_profile_as_default, lines 180-200` [low]
  - 现状：Async. Sets project.default_agent_profile_id = profile.profile_id when scope==PROJECT. Takes a WorkerProfile, reads only profile.scope/project_id/profile_id. No worker_profiles table read.
  - 目标：Change parameter type annotation WorkerProfile -> AgentProfile (caller now passes the unified object). Field accesses (scope, project_id, profile_id) are identical on AgentProfile — zero body change. Already writes default_agent_profile_id (correct unified key, no rename needed).
  - 注：Pure type-annotation swap; all accessed fields exist on AgentProfile.
- **apps/gateway/src/octoagent/gateway/services/control_plane/worker_profile_ops.py** `_worker_profile_control_capabilities, lines 267-369` [medium]
  - 现状：Takes status: WorkerProfileStatus; returns ControlPlaneCapability list whose capability_id/action_id are 'worker_profile.review|apply|publish|bind_default|clone|archive' + 'worker.spawn_from_profile'. Uses WorkerProfileStatus.ARCHIVED/ACTIVE comparisons.
  - 目标：(1) Rename param type WorkerProfileStatus -> AgentProfileStatus (enum values unchanged). (2) RENAME action_ids/capability_ids 'worker_profile.review'->'agent_profile.review', '.apply','.publish','.bind_default','.clone','.archive' likewise (these are the action_id rename targets in Wave 2 scope). LEAVE 'worker.spawn_from_profile' UNCHANGED (it is a worker.* runtime action, not worker_profile.*). CRITICAL: the action_id strings here MUST stay in lockstep with the dispatch map in worker_service.py (handler registration) AND any FE that posts these action_ids — coordinate the rename across worker_service handler keys + routes worker-profiles->agent-profiles + FE in the same wave or actions 404.
  - 注：action_id rename is the highest-coupling string change: it spans this file, worker_service dispatch keys, control_plane routes, and FE (FE is Wave 3 — so Wave 2 must keep BOTH old+new action_id accepted, OR Wave 2 explicitly defers action_id rename until FE wave. Decision needed.)
- **apps/gateway/src/octoagent/gateway/services/control_plane/worker_profile_ops.py** `_normalize_string_list (371-380), _slugify_worker_profile_token (382-399)` [low]
  - 现状：Pure string utilities. _slugify returns 'worker' fallback token. No DB/model access.
  - 目标：NO functional change. _slugify_worker_profile_token name + 'worker' fallback are cosmetic; keep (the slug is used to derive profile_id prefix 'worker-profile-...' which is persisted value format, must not change).
  - 注：profile_id prefix 'worker-profile-' (generated at line 411) is persisted identity — do NOT rename it, same Durability reasoning as worker-snapshot prefix.
- **apps/gateway/src/octoagent/gateway/services/control_plane/worker_profile_ops.py** `_generate_worker_profile_id, lines 401-417` [medium]
  - 现状：Async. Builds candidate id f'worker-profile-{scope_prefix}-{seed}'; checks collision via store.get_worker_profile(candidate); appends ULID suffix on collision.
  - 目标：Switch the collision-check read from store.get_worker_profile(candidate) -> store.get_agent_profile(candidate) (line 414). Because Wave 2 makes agent_profiles(kind=worker) the canonical store, uniqueness must be checked against the unified table. KEEP id format 'worker-profile-{...}' (persisted prefix). Subtle correctness note: get_agent_profile returns ANY kind row (main/subagent too) — but the 'worker-profile-' prefix means main/subagent ids never collide, so behavior is equivalent. Verify no main profile uses 'worker-profile-' prefix (it doesn't — main ids are project-derived).
  - 注：Read-source switch get_worker_profile->get_agent_profile. The collision semantics widen from worker-only to all-kinds, but prefix isolation keeps it safe — call out in review.
- **apps/gateway/src/octoagent/gateway/services/control_plane/worker_profile_ops.py** `_resolve_builtin_worker_source, lines 419-456` [high]
  - 现状：Async. For 'singleton:<worker_type>' ids, reads capability_pack builtin worker_profiles and constructs an in-memory WorkerProfile(... status=ACTIVE, origin_kind=BUILTIN, draft_revision=1, active_revision=1). Never touches DB. Returns WorkerProfile | None.
  - 目标：Change return type + construction WorkerProfile(...) -> AgentProfile(... kind='worker', persona_summary=summary, summary=summary, default_tool_groups/selected_tools/runtime_kinds populated, status=AgentProfileStatus.ACTIVE, origin_kind=AgentProfileOriginKind.BUILTIN, draft_revision=1, active_revision=1, scope=SYSTEM, project_id=''). MUST also stamp metadata source_kind='worker_profile_mirror' + behavior_agent_slug so is_worker_behavior_profile detects it on schema-less instances (it's an ephemeral object but downstream may persist/inspect it). Map fields: WorkerProfile.summary->AgentProfile.summary (AND persona_summary — note WorkerProfile has only summary; AgentProfile has BOTH persona_summary and the new summary field. The mirror builder at line 136-143 set persona_summary=summary AND summary=summary. Replicate that dual-assignment to preserve baseline: persona_summary=summary, summary=summary).
  - 注：Field-mapping subtlety: AgentProfile has TWO summary-ish fields (persona_summary legacy + summary new). Baseline mirror set both = WorkerProfile.summary. Builtin construction must do the same or builtin worker rendering diverges. Return-type change ripples to _get_worker_profile_in_scope + _review_worker_profile_draft signatures.
- **apps/gateway/src/octoagent/gateway/services/control_plane/worker_profile_ops.py** `_get_worker_profile_in_scope, lines 458-479` [high]
  - 现状：Async. Returns WorkerProfile. Tries builtin first (_resolve_builtin_worker_source), else store.get_worker_profile(profile_id), raises WORKER_PROFILE_NOT_FOUND/NOT_IN_SCOPE; enforces project scope. Called from ~9 worker_service handlers (clone/apply/publish/archive/bind/restore).
  - 目标：(1) Read-source switch: store.get_worker_profile -> store.get_agent_profile (line 466). (2) Return type WorkerProfile -> AgentProfile. (3) Scope check (lines 469-474: profile.scope/project_id) unchanged — fields identical on AgentProfile. IMPORTANT FILTER: get_agent_profile returns ANY kind; this helper must only resolve worker rows. Add a guard: if the resolved row is not a worker profile (use is_worker_behavior_profile from agent_decision, dual judgment kind OR metadata), raise WORKER_PROFILE_NOT_FOUND — otherwise a main/subagent id could be mutated through worker handlers. Builtin branch returns the AgentProfile from renamed _resolve_builtin_worker_source.
  - 注：The kind-guard is NEW behavior REQUIRED by the unified read: previously get_worker_profile physically could not return a main row. Without the guard, worker authoring handlers could clobber main/subagent profiles. This is the single most important correctness addition in the read switch.
- **apps/gateway/src/octoagent/gateway/services/control_plane/worker_profile_ops.py** `_review_worker_profile_draft, lines 487-730` [medium]
  - 现状：Async. Validation/diff engine. Signatures typed existing/source_profile: WorkerProfile|None, origin_kind: WorkerProfileOriginKind|None. Internally only model_dump(mode='json') on existing/source then dict.get on the JSON — so it is field-name driven, not attribute driven. Uses WorkerProfileOriginKind.BUILTIN/CUSTOM and WorkerProfileStatus.ARCHIVED. Builds 'normalized' dict + diff over snapshot_fields (name,summary,model_alias,tool_profile,default_tool_groups,selected_tools,runtime_kinds).
  - 目标：(1) Param types WorkerProfile->AgentProfile, WorkerProfileOriginKind->AgentProfileOriginKind. (2) Enum refs WorkerProfileOriginKind/WorkerProfileStatus -> AgentProfileOriginKind/AgentProfileStatus (values unchanged). (3) Body uses model_dump JSON keys — AgentProfile.model_dump includes the 9 absorbed fields under the SAME keys (summary, default_tool_groups, selected_tools, runtime_kinds, origin_kind, status, project_id, scope, name, model_alias, tool_profile, metadata, resource_limits, profile_id) PLUS extra main-only keys (persona_summary, kind, instruction_overlays, etc.) which the function ignores via explicit .get — so the .get-driven logic is SAFE with the wider dump. (4) existing.origin_kind / existing.status attribute access (lines 604, 670) work identically on AgentProfile. (5) _generate_worker_profile_id call (line 579) unchanged.
  - 注：Robust to the wider model_dump because it's allow-list .get-driven, NOT a full re-validate. One watch: existing_data.get('summary') now reads AgentProfile.summary (the new absorbed field) — Wave 1 populates it, so equivalent. Verify AgentProfile.model_dump doesn't expose persona_summary in a way that shadows 'summary' (separate keys, fine).
- **apps/gateway/src/octoagent/gateway/services/control_plane/worker_profile_ops.py** `_worker_profile_snapshot_payload, lines 732-747` [high]
  - 现状：Takes WorkerProfile; returns a dict (profile_id, scope.value, project_id, name, summary, model_alias, tool_profile, default_tool_groups, selected_tools, runtime_kinds, metadata, resource_limits, origin_kind.value). This is the revision snapshot stored in worker_profile_revisions.snapshot_payload AND spread into clone params (worker_service.py:799).
  - 目标：Param type WorkerProfile->AgentProfile. Body: every accessed field (scope, project_id, name, summary, model_alias, tool_profile, default_tool_groups, selected_tools, runtime_kinds, metadata, resource_limits, origin_kind) exists on AgentProfile with identical semantics — Wave 1 populated them. SHAPE MUST NOT CHANGE: snapshot_payload is persisted into agent_profile_revisions and replayed; it must keep EXACTLY the same keys/values as the old worker_profiles revisions so historical revisions remain comparable in _publish_worker_profile_revision's dedup check (latest.snapshot_payload == snapshot_payload, line 832). Do NOT add the new AgentProfile-only fields (persona_summary, kind, instruction_overlays) to the payload — that would (a) bloat stored revisions and (b) break the equality dedup against pre-migration revisions copied verbatim by migration_117. Keep the explicit field list verbatim.
  - 注：Snapshot shape question (prompt asks): ANSWER — shape must stay IDENTICAL. It's a hand-curated field list, not model_dump, precisely so it stays stable. The migration copies old worker_profile_revisions.snapshot_payload verbatim into agent_profile_revisions; if this builder starts emitting extra keys, the dedup equality at publish (line 830-836) breaks for the first post-migration publish, spuriously creating a new revision. Keep the list frozen.
- **apps/gateway/src/octoagent/gateway/services/control_plane/worker_profile_ops.py** `_save_worker_profile_draft, lines 749-816` [high]
  - 现状：Async. The core CREATE/UPDATE write. Computes status/draft_revision/active_revision lifecycle from existing WorkerProfile, constructs a WorkerProfile from normalized dict, persists via store.save_worker_profile, commits, returns WorkerProfile.
  - 目标：REWRITE to write the unified row: (1) param existing: WorkerProfile|None -> AgentProfile|None, origin_kind enum rename. (2) Lifecycle math (lines 768-785: DRAFT/ACTIVE, draft_revision/active_revision from existing.active_revision/draft_revision/status) is IDENTICAL on AgentProfile — keep verbatim. (3) Construct AgentProfile(kind='worker', ... 9 absorbed fields from normalized_profile, persona_summary=summary, summary=summary, status, origin_kind, draft/active_revision, archived_at=None) INSTEAD of WorkerProfile. (4) MUST stamp metadata: merge normalized metadata with source_kind='worker_profile_mirror' + behavior_agent_slug=normalize_behavior_agent_slug(name or profile_id) + worker_profile_id=profile_id (salvaged from deleted _build_agent_profile_from_worker_profile lines 119-129) so is_worker_behavior_profile works on schema-less live instances. (5) Persist via store.save_worker_profile -> store.save_agent_profile. (6) Fold in the materialize_agent_behavior_files(is_worker_profile=True) side-effect salvaged from deleted _sync (since _sync no longer called). (7) version field: must set version=max(existing.version,1) or it stays 1 — old WorkerProfile had no version concept; AgentProfile.save uses version. Default 1 is fine for create; on update preserve/increment to match save_agent_profile ON CONFLICT (it does NOT bump version, takes excluded.version) — pass existing.version to avoid resetting. Return AgentProfile.
  - 注：This is the heaviest rewrite. Three salvaged responsibilities land here: (a) metadata source_kind stamping for worker-detection, (b) behavior-file materialization, (c) persona_summary/summary dual-assign. Missing any of (a) breaks live-instance worker detection; (b) breaks agent-private behavior dirs; (c) breaks summary rendering. The AgentProfile.kind column is absent on live DBs (only via migration_117 ALTER) — so (a) is non-optional defense.
- **apps/gateway/src/octoagent/gateway/services/control_plane/worker_profile_ops.py** `_publish_worker_profile_revision, lines 818-864` [high]
  - 现状：Async. Reads revisions via store.list_worker_profile_revisions; computes snapshot via _worker_profile_snapshot_payload; dedup-checks latest revision (snapshot equality + revision number + ACTIVE status) returning (profile,latest,False) on no-op; else computes next_revision, persists WorkerProfileRevision via store.save_worker_profile_revision, then save_worker_profile with status=ACTIVE/active_revision/draft_revision, commits. Returns (WorkerProfile, WorkerProfileRevision, bool).
  - 目标：REWRITE: (1) return + param types WorkerProfile->AgentProfile, WorkerProfileRevision->AgentProfileRevision. (2) store.list_worker_profile_revisions -> store.list_agent_profile_revisions (NEW store method, Wave 2 adds; reads agent_profile_revisions table, same shape). (3) snapshot via renamed _worker_profile_snapshot_payload (shape frozen — see that entry). (4) Construct AgentProfileRevision(revision_id=self._worker_snapshot_id(...), profile_id, revision, change_summary, snapshot_payload, created_by, created_at) — revision_id format 'worker-snapshot:...' UNCHANGED (persisted value). (5) store.save_worker_profile_revision -> store.save_agent_profile_revision (NEW store method). (6) the final save_worker_profile(model_copy update status=ACTIVE...) -> save_agent_profile; model_copy update keys (status, active_revision, draft_revision, updated_at, archived_at) all valid AgentProfile fields. (7) MUST also re-stamp metadata source_kind on the model_copy if model_copy drops it? No — model_copy preserves metadata, and the saved profile came from the draft path which already stamped it. (8) Dedup equality (line 832) relies on snapshot shape stability — see _worker_profile_snapshot_payload risk. DEPENDENCY: store.save_agent_profile_revision + list_agent_profile_revisions do NOT yet exist (only worker_* variants at agent_context_store.py:227,258) — Wave 2 must add them (clone the worker_* impls, swap table name to agent_profile_revisions, swap row converter to AgentProfileRevision).
  - 注：Two NEW store methods are a hard prerequisite: save_agent_profile_revision / list_agent_profile_revisions targeting agent_profile_revisions (table + DDL already created by migration_117:476). The _row_to_worker_profile_revision converter (store:1462) must be cloned to _row_to_agent_profile_revision returning AgentProfileRevision. Without these the publish path has nowhere to write.
- **apps/gateway/src/octoagent/gateway/services/control_plane/worker_profile_ops.py** `module docstring, lines 1-15` [low]
  - 现状：Describes the mixin as 'worker profile 操作职责簇', references AgentProfile 镜像构建与同步 (mirror build/sync) as a responsibility.
  - 目标：Update docstring: remove '镜像构建与同步' (mirror build/sync no longer exists — unified write). Keep the rest. Cosmetic but required to avoid stale doc drift (project living-docs gate). Note the class WorkerProfileOpsMixin name itself is in the 'view classes WorkerProfile*->AgentProfile*' grey zone — recommend rename to AgentProfileOpsMixin only if worker_service.py base-class inheritance is renamed in lockstep; otherwise defer to a coordinated rename to avoid MRO breakage.
  - 注：Mixin class rename WorkerProfileOpsMixin->AgentProfileOpsMixin must coordinate with WorkerProfileDomainService base + worker_service.py inheritance — flag, don't rename unilaterally.
  - **gotchas**：KIND COLUMN ABSENT ON LIVE INSTANCES (KEY FACT a): agent_profiles has no 'kind' column until migration_117 ALTER runs; worker detection on live data falls back to metadata source_kind=='worker_profile_mirror' ONLY (is_worker_behavior_profile, agent_decision.py:112-125, dual judgment). Therefore the new draft/publish write path MUST keep stamping metadata['source_kind']='worker_profile_mirror' (+ behavior_agent_slug, worker_profile_id) — this is salvaged from the deleted _build_agent_profile_from_worker_profile (lines 119-129). Dropping it silently breaks worker detection. This is the #1 trap. / TWO SUMMARY FIELDS: AgentProfile has BOTH persona_summary (legacy) AND summary (new absorbed field). The baseline mirror builder set persona_summary=profile.summary AND summary=profile.summary (lines 136,143). Every place that constructs a worker AgentProfile (_save_worker_profile_draft, _resolve_builtin_worker_source) must replicate the dual-assignment or summary rendering diverges from baseline. / SNAPSHOT_PAYLOAD SHAPE MUST STAY FROZEN: _worker_profile_snapshot_payload (732-747) is a hand-curated 13-key dict, NOT model_dump. migration_117 copies old worker_profile_revisions.snapshot_payload verbatim into agent_profile_revisions. If the builder starts emitting AgentProfile-only keys (persona_summary/kind/instruction_overlays), the publish dedup equality check (line 830-836 latest.snapshot_payload==snapshot_payload) breaks against migrated revisions → spurious new revision on first post-migration publish. Keep the explicit field list verbatim; answer to 'does snapshot_payload need to change?' = NO. / NEW STORE METHODS ARE A HARD PREREQUISITE: save_agent_profile_revision + list_agent_profile_revisions + _row_to_agent_profile_revision do NOT exist yet (only worker_* at agent_context_store.py:227,258,1462). Wave 2 must add them targeting agent_profile_revisions (DDL already in migration_117:476-487, identical shape). _publish_worker_profile_revision cannot land before these. / KIND-GUARD ON _get_worker_profile_in_scope IS NEW REQUIRED BEHAVIOR: switching get_worker_profile->get_agent_profile widens the result to all kinds. Without a guard (raise WORKER_PROFILE_NOT_FOUND when not is_worker_behavior_profile), a main/subagent profile_id could be mutated through worker authoring handlers (clone/apply/publish/archive). Previously impossible because get_worker_profile physically only returned worker rows. / ARCHIVE-PATH STALE-MIRROR BUG (KEY FACT b) IS CLOSED STRUCTURALLY, NOT BY ADDING A SYNC CALL: _handle_worker_profile_archive (worker_service.py:855-870) currently saves worker_profiles without _sync, leaving the mirror stale. After Wave 2 the archive write goes directly to the unified agent_profiles row (single source), so status='archived' is authoritative — no second row to go stale. Verify the renamed archive handler writes via save_agent_profile (renamed save_worker_profile) and no longer references worker_profiles. / PERSISTED VALUE PREFIXES ARE OUT OF RENAME SCOPE: 'worker-snapshot:{...}' (revision_id + Work.effective_worker_snapshot_id) and 'worker-profile-{...}' (generated profile_id) are stored identity/audit values. Renaming them breaks Durability (stored revision_ids, Work audit, profile_ids). The Wave-2 rename scope is action_id/view-class/enum/route ONLY — leave these value formats untouched. effective_worker_snapshot_id is also a Work field (delegation_plane.py), entirely separate from WorkerProfileRevision. / ACTION_ID RENAME (worker_profile.*->agent_profile.*) IS HIGH-COUPLING + CROSS-WAVE: the action_ids in _worker_profile_control_capabilities (lines 277-360) must stay in lockstep with worker_service dispatch handler keys + control_plane routes (worker-profiles->agent-profiles) + FE (FE is Wave 3). Decision needed: either (a) accept BOTH old+new action_id during Wave 2-3, or (b) defer the action_id/route rename to the FE wave. The class/enum renames can land in Wave 2 safely; the wire-facing action_id strings are the risky part. 'worker.spawn_from_profile' is a worker.* runtime action and is NOT renamed. / WorkerProfileDynamicContext + WorkerProfileOpsMixin/WorkerProfileDomainService class names are in the grey zone of 'view classes WorkerProfile*->AgentProfile*'. The dynamic-context is a runtime UI VIEW (not the profile model) and the mixin/domain-service are inheritance-coupled to worker_service.py. Recommend: rename profile-model-mirroring view classes (the FE-facing WorkerProfile read models) but coordinate mixin/domain-service/dynamic-context renames separately to avoid MRO/import breakage. Needs an explicit include/exclude decision from the rename-pass owner.

## 维度：authoring domain file #2 — apps/gateway/src/octoagent/gateway/services/control_plane/worker_service.py (197 grep occurrences of worker_profile|WorkerProfile; ~176 logical sites). Maps the action_id->handler dispatch + document dispatch + 8 lifecycle handlers + 2 document producers, every WorkerProfile-model / view-class / enum usage, and the EXACT Wave 2 read+write switch (read unified agent_profiles row via get/list_agent_profiles; write unified agent_profiles + new agent_profile_revisions table via worker_profile_ops helpers; delete mirror builders; rename action_id worker_profile.*->agent_profile.*, resource keys worker_profiles->agent_profiles & worker-profiles->agent-profiles, view classes WorkerProfile*->AgentProfile*, enums WorkerProfileStatus/WorkerProfileOriginKind->AgentProfileStatus/AgentProfileOriginKind). CRITICAL archive-sync fix confirmed: _handle_worker_profile_archive (line 848) writes status=ARCHIVED via save_worker_profile to the mirror-less worker_profiles table — under Wave 2 it must write the unified agent_profiles(kind=worker) row, which closes the Wave-1-review stale-mirror gap automatically.

- **apps/gateway/src/octoagent/gateway/services/control_plane/worker_service.py** `lines 1-15 (module docstring) + class name line 65 WorkerProfileDomainService` [medium]
  - 现状：Docstring enumerates _build_agent_profile_from_worker_profile / _sync_worker_profile_agent_profile / _get_worker_profile_in_scope etc.; class named WorkerProfileDomainService(WorkerProfileOpsMixin, DomainServiceBase).
  - 目标：Rewrite docstring helper names to agent_profile.* per 全改名. Rename class WorkerProfileDomainService->AgentProfileDomainService (coordinate with _coordinator.py self._worker_service attribute + control_plane.py wiring — cross-file). Mixin import line 60 `from .worker_profile_ops import WorkerProfileOpsMixin` renames to AgentProfileOpsMixin when worker_profile_ops.py is renamed (separate dimension/file).
  - 注：Class rename ripples to _coordinator.py:653/671/800-801 and any control_plane.py construction site (out of this file). The coordinator dispatch table entries ('worker_profiles', self.get_worker_profiles_document) and get_worker_profile_revisions_document delegation must rename in lockstep or document routing breaks.
- **apps/gateway/src/octoagent/gateway/services/control_plane/worker_service.py** `imports lines 33-54 (octoagent.core.models)` [high]
  - 现状：Imports WorkerProfile, WorkerProfileOriginKind, WorkerProfileRevisionItem, WorkerProfileRevisionsDocument, WorkerProfilesDocument, WorkerProfileStaticConfig, WorkerProfileStatus, WorkerProfileViewItem (8 symbols) alongside AgentProfile, AgentProfileScope.
  - 目标：After view/enum classes are renamed in core models (agent_context.py:80/86 + control_plane/agent.py:39/70/93/101/111), swap these 8 imports to AgentProfileOriginKind, AgentProfileRevisionItem, AgentProfileRevisionsDocument, AgentProfilesDocument, AgentProfileStaticConfig, AgentProfileStatus, AgentProfileViewItem. WorkerProfile model itself: in Wave 2 the authoring path reads/writes the unified AgentProfile, so the bare `WorkerProfile` import + every `WorkerProfile | None` annotation (lines 616-617, 903) should become `AgentProfile | None` IF helpers return AgentProfile. NOTE: keep WorkerProfile class itself alive (Wave 4 deletes it) — but this file should stop importing it once helpers return AgentProfile.
  - 注：Decision coupling: whether _get_worker_profile_in_scope / _save_worker_profile_draft (worker_profile_ops.py) return WorkerProfile or AgentProfile in Wave 2 determines whether these annotations flip. worker_profile_ops.py is a SEPARATE dimension; this file's annotations must match that contract. If helpers still return WorkerProfile in Wave 2, keep the import but the read path must hydrate from unified table.
- **apps/gateway/src/octoagent/gateway/services/control_plane/worker_service.py** `action_routes() lines 75-89` [high]
  - 现状：dispatch map keys 'worker_profile.review'/'.create'/'.update'/'.clone'/'.archive'/'.apply'/'.publish'/'.bind_default' -> 8 _handle_worker_profile_* handlers; plus 'worker.spawn_from_profile', 'worker.extract_profile_from_runtime', 'behavior.read_file', 'behavior.write_file'.
  - 目标：Rename 8 action_id keys worker_profile.*->agent_profile.* AND rename the 8 handler methods _handle_worker_profile_*->_handle_agent_profile_*. Leave 'worker.spawn_from_profile'/'worker.extract_profile_from_runtime' (Work-spawn verbs, not profile-entity) and behavior.* keys unchanged unless 全改名 spec says spawn verbs too — they target Works not profiles, so EXCLUDE from rename.
  - 注：action_id strings are an FE↔backend contract; FE is NOT in this worktree (no apps/web). Renaming action_id worker_profile.create->agent_profile.create breaks FE callers until Wave 3 FE rename lands in lockstep. Flag as required-coordinated cutover. capability_pack.py may also advertise these action_ids in capability descriptors — grep capability_pack for 'worker_profile.' before renaming.
- **apps/gateway/src/octoagent/gateway/services/control_plane/worker_service.py** `document_routes() lines 91-95` [high]
  - 现状：{'worker_profiles': self.get_worker_profiles_document, 'worker_profile_revisions': self.get_worker_profile_revisions_document}.
  - 目标：Rename resource keys 'worker_profiles'->'agent_profiles' and 'worker_profile_revisions'->'agent_profile_revisions'; rename method get_worker_profiles_document->get_agent_profiles_document and get_worker_profile_revisions_document->get_agent_profile_revisions_document.
  - 注：Coordinator _coordinator.py:653 tuple ('worker_profiles', self.get_worker_profiles_document) and :800-801 delegation, plus routes/control_plane.py:44 GET /resources/worker-profiles and :51 GET /resources/worker-profile-revisions/{profile_id} -> /resources/agent-profiles[-revisions]. ALL must rename together or the resource lookup 404s. FE consumes /api/control/resources/worker-profiles.
- **apps/gateway/src/octoagent/gateway/services/control_plane/worker_service.py** `get_worker_profiles_document() lines 101-387` [high]
  - 现状：READS via self._stores.agent_context_store.list_worker_profiles(include_archived=True) (line 105). Builds items via WorkerProfileViewItem (172, 259), WorkerProfileStaticConfig (196, 272). Calls _build_agent_profile_from_worker_profile (line 152) to synthesize a behavior mirror for each stored profile. Branches on WorkerProfileStatus.ARCHIVED/ACTIVE/DRAFT (140, 266, 293, 354, 357, 360) and WorkerProfileOriginKind.BUILTIN (190, 265, 343, 350). Iterates capability_pack.pack.worker_profiles (221) for builtin singletons. work.requested_worker_profile_id (115-116) used for work matching. Returns WorkerProfilesDocument (333) with capability_id/action_id 'worker_profile.create' (374, 376) and refs revisions_base '/api/control/resources/worker-profile-revisions/{profile_id}' (380).
  - 目标：Switch read to list_agent_profiles(...) filtered to worker-kind rows (kind=worker OR metadata.source_kind='worker_profile_mirror' via is_worker_behavior_profile — live DBs have NO kind column, so detection MUST go through is_worker_behavior_profile, agent_decision.py:112). DELETE the _build_agent_profile_from_worker_profile call at line 152 — the row IS already an AgentProfile; pass it straight to materialize/build_behavior_system_summary. Rename view classes WorkerProfileViewItem->AgentProfileViewItem, WorkerProfileStaticConfig->AgentProfileStaticConfig, enums WorkerProfileStatus->AgentProfileStatus, WorkerProfileOriginKind->AgentProfileOriginKind, WorkerProfilesDocument->AgentProfilesDocument. Rewrite action_id/capability_id 'worker_profile.create'->'agent_profile.create' (374,376) and refs 'worker-profile-revisions'->'agent-profile-revisions' (380). EXCLUDE work.requested_worker_profile_id (Work-model field, Wave 4 not Wave 2). EXCLUDE capability_pack.pack.worker_profiles (builtin archetype list, separate concept — stays).
  - 注：The 9 absorbed fields (summary/default_tool_groups/selected_tools/runtime_kinds/status/origin_kind/draft_revision/active_revision/archived_at) are now read directly off the AgentProfile row (Wave 0 added them) — lines 178-202 already read profile.status/origin_kind/active_revision/draft_revision/summary/default_tool_groups/selected_tools/runtime_kinds/metadata/resource_limits, which is exactly the unified set, so the field-access code is reuse-ready once the row source flips. Keep is_worker_profile=True kwargs (162, 249) to materialize_agent_behavior_files UNCHANGED — that flag is a behavior-template selector (worker-variant SOUL/HEARTBEAT), not a table-rename target.
- **apps/gateway/src/octoagent/gateway/services/control_plane/worker_service.py** `get_worker_profiles_document() lines 235-242 (builtin synthetic AgentProfile) + 321-331 (default fallback)` [medium]
  - 现状：Builtin singleton path already constructs a real AgentProfile (235) for behavior_system. Lines 326-331 fall back to agent_context_store.get_agent_profile(default_profile_id) when WorkerProfile lookup misses — comment cites Bug 075 that default_profile_id may point to an AgentProfile not in WorkerProfiles list.
  - 目标：Builtin synthetic AgentProfile (235-242) stays as-is. The Bug-075 fallback at 326-331 becomes redundant once the primary list is already AgentProfile rows from the unified table (the default_profile_id will be present in the unified list) — but keep it defensively until verified; do NOT delete in Wave 2 without confirming the default always resolves from list_agent_profiles.
  - 注：This fallback is evidence the two tables were already partially unified at read time pre-F117; Wave 2 makes it the single source. Verify default_profile_id resolution before pruning.
- **apps/gateway/src/octoagent/gateway/services/control_plane/worker_service.py** `get_worker_profile_revisions_document() lines 389-497` [high]
  - 现状：READS stored_profile via agent_context_store.get_worker_profile(profile_id) (394) and revisions via list_worker_profile_revisions(profile_id) (409). Builds WorkerProfileRevisionItem (413, 443) and WorkerProfileRevisionsDocument (467). resource_id f'worker-profile-revisions:{profile_id}' (468). capability_id/action_id 'worker_profile.publish' (478, 480). degraded reason 'worker_profile_revisions_empty' (495). Scope-check raises 'WORKER_PROFILE_NOT_IN_SCOPE' (406). singleton: branch reads capability_pack.pack.worker_profiles (432).
  - 目标：Switch reads to get_agent_profile(profile_id) + list_agent_profile_revisions(profile_id) (new store method on the agent_profile_revisions table). Rename WorkerProfileRevisionItem->AgentProfileRevisionItem, WorkerProfileRevisionsDocument->AgentProfileRevisionsDocument. resource_id->'agent-profile-revisions:{profile_id}'. action_id/capability_id 'worker_profile.publish'->'agent_profile.publish'. degraded reason + error code strings 'worker_profile_*'->'agent_profile_*' per 全改名. Keep capability_pack.pack.worker_profiles (builtin archetype — stays).
  - 注：Requires a new store method list_agent_profile_revisions on agent_context_store (currently only list_worker_profile_revisions at agent_context_store.py:258). That store dimension is separate; this file CONSUMES it. The agent_profile_revisions table is created by migration_117 (confirmed line 476) and DROP of worker_profile_revisions at line 540.
- **apps/gateway/src/octoagent/gateway/services/control_plane/worker_service.py** `_handle_worker_profile_review() lines 608-666` [medium]
  - 现状：source_profile/existing typed WorkerProfile|None (616-617). Loads via _get_worker_profile_in_scope (622, 626). Branches WorkerProfileOriginKind.BUILTIN (629), .CLONED (639). Calls _review_worker_profile_draft. resource_refs 'worker_profiles'/'worker_profile_revisions' (649-652). target_type='worker_profile' (658). code WORKER_PROFILE_REVIEW_READY (645).
  - 目标：Rename method ->_handle_agent_profile_review. Flip annotations to AgentProfile|None (if helpers return AgentProfile). Rename enums ->AgentProfileOriginKind. resource_refs ->'agent_profiles'/'agent_profile_revisions' + 'agent-profiles:overview'/'agent-profile-revisions:'. target_type->'agent_profile'. code WORKER_PROFILE_REVIEW_READY->AGENT_PROFILE_REVIEW_READY.
  - 注：_review_worker_profile_draft lives in worker_profile_ops.py (separate dimension); its rename to _review_agent_profile_draft must coordinate. target_type='worker_profile' (10 occurrences total across file) is a ControlPlaneTargetRef discriminator FE may switch on — coordinate rename with FE (Wave 3).
- **apps/gateway/src/octoagent/gateway/services/control_plane/worker_service.py** `_handle_worker_profile_create() lines 668-729` [high]
  - 现状：Existence-check via agent_context_store.get_worker_profile(profile_id) (676). origin_kind WorkerProfileOriginKind.CUSTOM (687, 695). Calls _review_worker_profile_draft + _save_worker_profile_draft (692). materialize_agent_behavior_files(is_worker_profile=True) (698-703). resource_refs/target_refs worker_profile (716-727). Error codes WORKER_PROFILE_ALREADY_EXISTS (679), WORKER_PROFILE_CREATE_INVALID (691). code WORKER_PROFILE_CREATED (706).
  - 目标：Rename ->_handle_agent_profile_create. Existence-check via get_agent_profile (worker-kind). _save_worker_profile_draft->_save_agent_profile_draft (helper writes UNIFIED agent_profiles + agent_profile_revisions instead of worker_profiles). enum CUSTOM->AgentProfileOriginKind.CUSTOM. Rename resource/target strings + error codes worker_profile->agent_profile. KEEP is_worker_profile=True kwarg.
  - 注：The write switch lives in _save_worker_profile_draft (worker_profile_ops.py:749, currently calls save_worker_profile at 786) — this handler just calls it. Wave 2 correctness hinges on that helper writing the unified table; this file's only obligation is to call the renamed helper and stop using get_worker_profile for existence.
- **apps/gateway/src/octoagent/gateway/services/control_plane/worker_service.py** `_handle_worker_profile_update() lines 731-785` [medium]
  - 现状：Loads existing via _get_worker_profile_in_scope (740). Blocks WorkerProfileOriginKind.BUILTIN (741). _review_worker_profile_draft + _save_worker_profile_draft (756). resource/target worker_profile strings. Error WORKER_PROFILE_REQUIRED (739), WORKER_PROFILE_UPDATE_INVALID (755). code WORKER_PROFILE_UPDATED (762).
  - 目标：Rename ->_handle_agent_profile_update. Helper calls ->_get_agent_profile_in_scope / _save_agent_profile_draft. enum BUILTIN->AgentProfileOriginKind.BUILTIN. Rename all worker_profile->agent_profile strings/codes.
  - 注：Pure delegation + string rename; correctness delegated to ops helpers (separate dimension).
- **apps/gateway/src/octoagent/gateway/services/control_plane/worker_service.py** `_handle_worker_profile_clone() lines 787-846` [medium]
  - 现状：Loads source via _get_worker_profile_in_scope (797). Builds raw from _worker_profile_snapshot_payload (799). raw['origin_kind']=WorkerProfileOriginKind.CLONED.value (805). _review_worker_profile_draft + _save_worker_profile_draft (817) origin_kind CLONED (812, 820). Error WORKER_PROFILE_SOURCE_REQUIRED (794), WORKER_PROFILE_CLONE_INVALID (816). code WORKER_PROFILE_CLONED (824).
  - 目标：Rename ->_handle_agent_profile_clone. helpers ->_get_agent_profile_in_scope/_worker_profile_snapshot_payload->_agent_profile_snapshot_payload/_save_agent_profile_draft. enum CLONED->AgentProfileOriginKind.CLONED. Rename strings/codes worker_profile->agent_profile.
  - 注：_worker_profile_snapshot_payload (worker_profile_ops.py:732) renames in its own dimension; this handler consumes it.
- **apps/gateway/src/octoagent/gateway/services/control_plane/worker_service.py** `_handle_worker_profile_archive() lines 848-893 — CRITICAL ARCHIVE-SYNC FIX` [high]
  - 现状：Loads existing via _get_worker_profile_in_scope (855), blocks BUILTIN (856). WRITES status=WorkerProfileStatus.ARCHIVED + archived_at via self._stores.agent_context_store.save_worker_profile(existing.model_copy(update={'status': WorkerProfileStatus.ARCHIVED, 'archived_at': now, 'updated_at': now})) (861-869) then conn.commit() (870). This writes ONLY the worker_profiles table — Wave 1 review confirmed it does NOT sync the agent_profiles(kind=worker) mirror, so the mirror status stays stale (non-ARCHIVED). resource/target/code WORKER_PROFILE_ARCHIVED (873).
  - 目标：Rename ->_handle_agent_profile_archive. Replace save_worker_profile(...) call with save_agent_profile(...) (or _save-equivalent) writing status=AgentProfileStatus.ARCHIVED + archived_at to the UNIFIED agent_profiles(kind=worker) row identified by existing.profile_id. Because read path now reads the same unified row, writing ARCHIVED to it CLOSES the stale-mirror gap by construction — there is no longer a separate mirror to drift. enum WorkerProfileStatus.ARCHIVED->AgentProfileStatus.ARCHIVED. Rename strings/codes. KEEP archived_at/updated_at field writes (Wave 0 absorbed archived_at into AgentProfile).
  - 注：THIS is the dimension's critical fix. Verify save_agent_profile persists the status/origin_kind/draft_revision/active_revision/archived_at columns (Wave 0 store change at agent_context_store.py:43 must round-trip them). If save_agent_profile drops any absorbed field, archive will silently lose status. Also confirm there is no SECOND archive path elsewhere (e.g. agent_service.py) writing worker_profiles independently. The pre-existing get_worker_profiles_document already filters ARCHIVED via profile.status (line 140) — so once archive writes the unified status, the list view hides it correctly with zero extra code.
- **apps/gateway/src/octoagent/gateway/services/control_plane/worker_service.py** `_handle_worker_profile_apply() lines 895-992` [high]
  - 现状：existing WorkerProfile|None (903). Loads via get_worker_profile (906), blocks BUILTIN (908). _review_worker_profile_draft + _save_worker_profile_draft (924). On publish: _publish_worker_profile_revision (940) + _sync_worker_profile_agent_profile (948) + _bind_worker_profile_as_default (963). conn.commit (971). resource_refs incl 'delegation_plane' (983). Error codes WORKER_PROFILE_BUILTIN_READONLY (910), WORKER_PROFILE_APPLY_INVALID (923), WORKER_PROFILE_REVIEW_BLOCKED (939). code WORKER_PROFILE_APPLIED (974).
  - 目标：Rename ->_handle_agent_profile_apply. helpers ->get_agent_profile/_save_agent_profile_draft/_publish_agent_profile_revision/_bind_agent_profile_as_default. CRITICAL: _sync_worker_profile_agent_profile (948) — in Wave 2 the row IS the AgentProfile, so this mirror-sync helper becomes a NO-OP / is DELETED (the prompt mandates deleting mirror builders). Drop the _sync_*_agent_profile call entirely and ensure _publish helper persists the unified row + agent_profile_revisions. enum BUILTIN/AgentProfileScope.PROJECT unchanged (PROJECT is AgentProfileScope already). Rename strings/codes.
  - 注：_sync_worker_profile_agent_profile (worker_profile_ops.py:157) calls _build_agent_profile_from_worker_profile + save_agent_profile — that whole helper is a mirror builder slated for deletion in Wave 2. Removing its call here is REQUIRED; if left, it double-writes / re-mirrors a row that is already canonical. set_as_default branch (953-963) and _bind_worker_profile_as_default (ops:180, writes project.default_agent_profile_id) keep working unchanged in behavior, just renamed.
- **apps/gateway/src/octoagent/gateway/services/control_plane/worker_service.py** `_handle_worker_profile_publish() lines 994-1074` [high]
  - 现状：Draft-mapping path delegates to _handle_worker_profile_apply with action_id 'worker_profile.apply' and publish=True (1000-1002). Else loads existing via _get_worker_profile_in_scope (1007), blocks BUILTIN (1008). _review_worker_profile_draft mode='publish' (1014). _publish_worker_profile_revision (1023) + _sync_worker_profile_agent_profile (1031) + _bind_worker_profile_as_default (1047). conn.commit (1048). Error WORKER_PROFILE_REQUIRED (1006), WORKER_PROFILE_REVIEW_BLOCKED (1022). code WORKER_PROFILE_PUBLISHED (1051).
  - 目标：Rename ->_handle_agent_profile_publish. The internal request.model_copy(update={'action_id': 'worker_profile.apply', ...}) (1001) MUST rename the literal action_id to 'agent_profile.apply' AND call the renamed _handle_agent_profile_apply. helpers ->_get_agent_profile_in_scope/_publish_agent_profile_revision/_bind_agent_profile_as_default. DELETE the _sync_worker_profile_agent_profile call (1031) — mirror builder removed. Rename strings/codes.
  - 注：The hardcoded action_id literal 'worker_profile.apply' at line 1001 is an easy miss — it re-enters the dispatch indirectly via model_copy; if action_routes key renamed to 'agent_profile.apply' but this literal not updated, the re-dispatched request carries a now-unknown action_id (though it calls apply directly, the envelope.action_id is logged/audited). Update for consistency. Same _sync mirror-builder deletion as apply.
- **apps/gateway/src/octoagent/gateway/services/control_plane/worker_service.py** `_handle_worker_profile_bind_default() lines 1076-1119` [high]
  - 现状：Loads existing via _get_worker_profile_in_scope (1083), blocks BUILTIN (1084). Requires status WorkerProfileStatus.ACTIVE (1089). _sync_worker_profile_agent_profile (1095) + _bind_worker_profile_as_default (1096). conn.commit (1097). resource_refs incl 'agent_profiles'/'setup_governance' (1109-1110). Error WORKER_PROFILE_REQUIRED (1082), WORKER_PROFILE_BIND_UNSUPPORTED (1086), WORKER_PROFILE_NOT_PUBLISHED (1091). code WORKER_PROFILE_BOUND_DEFAULT (1100).
  - 目标：Rename ->_handle_agent_profile_bind_default. enum WorkerProfileStatus.ACTIVE->AgentProfileStatus.ACTIVE. DELETE _sync_worker_profile_agent_profile call (1095) — mirror removed; the unified row is already canonical, so binding default_agent_profile_id (via _bind_agent_profile_as_default) needs no prior mirror-sync. helpers renamed. Rename strings/codes. NOTE resource_ref 'agent_profiles' (1109) already correct — leave; 'worker_profiles' (1108) -> 'agent_profiles'.
  - 注：Removing _sync at 1095 is safe ONLY if the unified row already exists (it does — bind requires an ACTIVE published profile). Confirm _bind_agent_profile_as_default reads profile.scope==AgentProfileScope.PROJECT (ops:185) which is already AgentProfile-native.
- **apps/gateway/src/octoagent/gateway/services/control_plane/worker_service.py** `_handle_worker_spawn_from_profile() lines 1121-1199` [medium]
  - 现状：Loads profile via _get_worker_profile_in_scope (1128), blocks WorkerProfileStatus.ARCHIVED (1129). Builds NormalizedMessage.control_metadata with keys requested_worker_profile_id (1155), requested_worker_profile_version (1156), effective_worker_snapshot_id (1157), requested_worker_type (1161), target_kind (1163). Launches via task_runner.launch_child_task. resource_refs/target worker_profile (1189-1197). Error WORKER_PROFILE_REQUIRED (1127), WORKER_PROFILE_ARCHIVED (1131). code WORKER_PROFILE_SPAWNED (1178).
  - 目标：Rename ->_handle_agent_spawn_from_profile? NO — action_id is 'worker.spawn_from_profile' (a Work-spawn verb, EXCLUDED from worker_profile->agent_profile rename). Keep method/action_id as worker.spawn_from_profile per scope (it spawns a Work, not authors a profile). DO flip _get_worker_profile_in_scope->_get_agent_profile_in_scope and enum WorkerProfileStatus.ARCHIVED->AgentProfileStatus.ARCHIVED and resource_ref 'worker_profiles'->'agent_profiles'. DO NOT rename control_metadata keys requested_worker_profile_id/_version/effective_worker_snapshot_id/requested_worker_type — these are Work-model contract fields (Wave 4 / out of scope). target_type='worker_profile' (1194) renames with the other 9 if FE coordinates.
  - 注：This handler straddles the boundary: profile-entity reads (rename) vs Work-spawn control_metadata (keep). The control_metadata keys map to Work.requested_worker_profile_id which Wave 4 handles — renaming them here would desync from the Work model. Explicitly EXCLUDE lines 1155-1161 key strings.
- **apps/gateway/src/octoagent/gateway/services/control_plane/worker_service.py** `_handle_worker_extract_profile_from_runtime() lines 1201-1268` [medium]
  - 现状：action_id 'worker.extract_profile_from_runtime'. Loads work via _get_work_in_scope (1208). Builds raw draft with _worker_profile_label (1212). origin_kind WorkerProfileOriginKind.EXTRACTED (1233, 1241). _review_worker_profile_draft + _save_worker_profile_draft (1238). raw['metadata'] carries source_work_id/source_task_id/source_snapshot_id. resource/target worker_profile (1253-1266). Error WORK_ID_REQUIRED (1207), WORKER_PROFILE_EXTRACT_INVALID (1237). code WORKER_PROFILE_EXTRACTED (1245).
  - 目标：Keep action_id 'worker.extract_profile_from_runtime' (Work-derived verb, EXCLUDED like spawn). Flip helpers _review_worker_profile_draft->_review_agent_profile_draft, _save_worker_profile_draft->_save_agent_profile_draft (writes unified table), _worker_profile_label->_agent_profile_label (or keep label helper as-is if it's builtin-archetype labeling). enum EXTRACTED->AgentProfileOriginKind.EXTRACTED. Rename resource 'worker_profiles'->'agent_profiles', code WORKER_PROFILE_EXTRACTED->AGENT_PROFILE_EXTRACTED. KEEP source_* metadata keys (Work provenance).
  - 注：_worker_profile_label (worker_profile_ops.py:77) is used both for builtin archetype labels (worker_type) and extract drafts — its rename is ambiguous; if it stays label-for-builtin it may keep worker naming. Coordinate with ops dimension. work.target_kind.value membership check {'worker','subagent',...} (1218) is runtime_kind values — unrelated to profile rename, keep.
  - **gotchas**：ARCHIVE-SYNC FIX CONFIRMED (the critical ask): _handle_worker_profile_archive at line 848 currently calls self._stores.agent_context_store.save_worker_profile(existing.model_copy(update={'status': WorkerProfileStatus.ARCHIVED, 'archived_at': ..., 'updated_at': ...})) at lines 861-869 — it writes ONLY worker_profiles and never touches the agent_profiles(kind=worker) mirror, so the mirror status stays non-ARCHIVED (Wave 1 review's stale-mirror finding). Wave 2 fix: replace save_worker_profile with save_agent_profile writing status=AgentProfileStatus.ARCHIVED+archived_at to the unified agent_profiles row by profile_id. Because the read path (get_worker_profiles_document line 105/140) now reads that same unified row and already hides ARCHIVED via profile.status, the fix closes the gap by construction with NO extra mirror-sync code. / MIRROR-BUILDER DELETIONS in THIS file: (1) get_worker_profiles_document line 152 calls _build_agent_profile_from_worker_profile per stored profile — DELETE, pass the unified AgentProfile row straight through. (2) _sync_worker_profile_agent_profile is called in THREE handlers: _handle_worker_profile_apply line 948, _handle_worker_profile_publish line 1031, _handle_worker_profile_bind_default line 1095 — ALL THREE calls must be DELETED in Wave 2 (the row is already canonical AgentProfile; re-mirroring it double-writes). The helper itself (worker_profile_ops.py:157) is deleted in its own dimension. / WORKER-DETECTION MUST use is_worker_behavior_profile (agent_decision.py:112), NOT a kind-column equality check — live instance DBs have NO 'kind' column on agent_profiles; worker mirrors are detected via metadata.source_kind='worker_profile_mirror' OR kind (dual judgment). The Wave 2 read switch in get_worker_profiles_document/get_worker_profile_revisions_document must filter list_agent_profiles to worker-kind rows through this helper, not `kind == 'worker'`. / NEW STORE METHOD DEPENDENCY: this file CONSUMES list_agent_profile_revisions + a save path to the agent_profile_revisions table (migration_117 creates it at migration line 476, DROPs worker_profile_revisions at 540). agent_context_store.py currently only has list_worker_profile_revisions (line 258) / save_worker_profile_revision (227). The store-method rename/addition is a SEPARATE dimension; if it lands after this file, get_worker_profile_revisions_document (line 409) breaks. Sequence the store dimension before/with this one. / ACTION_ID / RESOURCE-KEY / target_type RENAMES ARE AN FE CONTRACT and FE is NOT in this worktree (no apps/web found). Renaming action_routes keys worker_profile.*->agent_profile.* (lines 77-84), document_routes keys worker_profiles/worker_profile_revisions->agent_profiles/agent_profile_revisions (93-94), the GET routes /resources/worker-profiles[-revisions] (control_plane.py:44,51), resource_ref('worker_profiles','worker-profiles:overview') (24 sites), and target_type='worker_profile' (10 sites) all break FE callers until Wave 3 FE rename lands. The prompt defers FE to Wave 3 — so either (a) Wave 2 renames backend strings and Wave 3 FE must ship in lockstep (contract break window), or (b) backend keeps the old action_id/resource strings as the wire contract and renames only Python symbols. Flag for the orchestrator: decide whether wire-strings rename in Wave 2 or stay until Wave 3. / HARDCODED action_id LITERAL at line 1001: _handle_worker_profile_publish does request.model_copy(update={'action_id': 'worker_profile.apply', ...}) — this string literal is NOT in the action_routes map declaration and is easy to miss in a mechanical rename. If action_id keys rename to agent_profile.* update this literal too. / DO NOT RENAME is_worker_profile= kwarg (lines 162, 249, 702) passed to materialize_agent_behavior_files — it is a behavior-template selector (worker-variant SOUL.worker.md/HEARTBEAT.worker.md), defined in core behavior_workspace/skeleton.py:183, NOT a worker_profiles-table reference. Renaming it would break behavior file materialization. Same for is_worker=is_worker_profile downstream (skeleton.py:223). / DO NOT RENAME Work-model control_metadata / field references: work.requested_worker_profile_id (lines 115-116), control_metadata keys requested_worker_profile_id/requested_worker_profile_version/effective_worker_snapshot_id/requested_worker_type (lines 1155-1161). These map to the Work model (Wave 4 / out of scope), not the profile entity. Also EXCLUDE capability_pack.pack.worker_profiles (lines 221, 432) — that is the builtin archetype list (WorkerCapabilityProfile, capability_pack.py:864), a distinct concept that stays. / ACTION_ID VERBS worker.spawn_from_profile (line 85) and worker.extract_profile_from_runtime (line 86) are Work-spawn/extract verbs, NOT profile-authoring — EXCLUDE from the worker_profile.*->agent_profile.* action_id rename. Their handler bodies still rename their internal profile-reads (_get_worker_profile_in_scope etc.) and enums, but the action_id keys + method names tied to 'worker.' Work verbs should remain unless the 全改名 spec explicitly extends to Work verbs. / RETURN-TYPE CONTRACT WITH worker_profile_ops.py (separate dimension): every `WorkerProfile | None` annotation (lines 616-617, 903) and the bare `WorkerProfile` import flip to AgentProfile ONLY IF the ops helpers (_get_worker_profile_in_scope, _save_worker_profile_draft, _publish_worker_profile_revision) are switched to return AgentProfile in Wave 2. If ops still returns WorkerProfile (hydrated from unified table), keep these annotations. This file's annotations MUST match the ops-dimension contract — sequence/agree the return type first. / conn.commit() is called explicitly in archive (870), apply-publish (971), publish (1048), bind_default (1097) — the Wave 2 write switch must preserve these commit boundaries; the unified-table writes (save_agent_profile + agent_profile_revisions insert) must be inside the same transaction before commit, else partial-write on crash (Constitution #1 Durability). _save_worker_profile_draft (ops:815) and _publish_worker_profile_revision (ops:863) already commit internally — verify no double-commit / nested-txn issue after switch.

## 维度：store + archive-sync + coordinator + other consumers

- **packages/core/src/octoagent/core/store/agent_context_store.py** `save_worker_profile_revision (227-256) + list_worker_profile_revisions (258-270) + _row_to_worker_profile_revision (1462-1475)` [low]
  - 现状：Only worker_profile_revisions table has store methods. save_worker_profile_revision INSERTs into worker_profile_revisions; list reads from it; _row_to_worker_profile_revision builds WorkerProfileRevision from a row. No agent_profile_revisions methods exist.
  - 目标：ADD save_agent_profile_revision + list_agent_profile_revisions, exact mirrors of the worker versions but targeting the agent_profile_revisions table. Reuse the SAME column set (revision_id, profile_id, revision, change_summary, snapshot_payload, created_by, created_at) and the SAME WorkerProfileRevision view model (which 全改名 renames to AgentProfileRevision) + same _row_to_*_revision converter (rename to _row_to_agent_profile_revision). The authoring switch (worker_profile_ops.py:825/841) calls these new methods instead of the worker ones. Keep worker_* revision methods for now (Wave 4 deletes them with the worker_profiles tables).
  - 注：agent_profile_revisions is referenced ONLY in migration_117 today (CREATE at migration_117_profile_merge.py:474-486 with FK→agent_profiles + UNIQUE(profile_id,revision)). Wave 2 must use the IDENTICAL DDL so live (post-migrate) and fresh-init (sqlite_init) schemas match exactly.
- **packages/core/src/octoagent/core/store/sqlite_init.py** `_WORKER_PROFILE_REVISIONS_DDL (436-449) + CREATE-table block (1787) + _AGENT_CONTEXT_INDEXES revision index (1023-1026)` [medium]
  - 现状：sqlite_init creates worker_profiles, worker_profile_revisions, agent_profiles tables but has NO agent_profile_revisions table. The agent_profile_revisions table is created only by migration_117 apply. A fresh DB (no migration run) would have agent_profile_revisions missing → save_agent_profile_revision would fail.
  - 目标：ADD an _AGENT_PROFILE_REVISIONS_DDL constant byte-identical to migration_117's CREATE (revision_id TEXT PK, profile_id TEXT NOT NULL, revision INTEGER NOT NULL, change_summary TEXT DEFAULT '', snapshot_payload TEXT DEFAULT '{}', created_by TEXT DEFAULT '', created_at TEXT NOT NULL, FOREIGN KEY(profile_id) REFERENCES agent_profiles(profile_id), UNIQUE(profile_id,revision)). Execute it in the CREATE block (alongside _AGENT_PROFILES_DDL at ~1785-1788). ADD an index idx_agent_profile_revisions_profile_created ON agent_profile_revisions(profile_id, revision DESC, created_at DESC) into _AGENT_CONTEXT_INDEXES (mirror line 1023-1026). The F117 ALTER block (1237-1256) already self-contains the 9-field column backfill, so no column change needed.
  - 注：Without this, the read-switch + authoring-write-switch breaks on any DB that has NOT had migration_117 applied (e.g. fresh test DBs, fresh installs). This is the single most load-bearing DDL gap for Wave 2.
- **apps/gateway/src/octoagent/gateway/services/control_plane/worker_service.py** `_handle_worker_profile_archive (848-893), esp. lines 861-870` [high]
  - 现状：Archive saves status=ARCHIVED + archived_at ONLY to worker_profiles via save_worker_profile (861-869). It does NOT call _sync_worker_profile_agent_profile, so the unified agent_profiles(kind=worker) mirror keeps stale status=active/archived_at=null. Wave 1 review flagged this exactly: mirror status goes stale on archive.
  - 目标：After the read-switch, capability_pack/chat/session read the unified agent_profiles row, so archive MUST sync it. Add a sync write of the unified row's status=ARCHIVED + archived_at after the worker_profiles update — either by calling self._sync_worker_profile_agent_profile(archived, revision=archived.active_revision or archived.draft_revision) before commit, or (cleaner once authoring writes the unified table directly) write status/archived_at straight onto the agent_profiles row via save_agent_profile. Read-paths gate on status==ARCHIVED, so without this the archived worker stays dispatchable.
  - 注：This is THE archive-sync gate. All read consumers (capability_pack.resolve_worker_binding:413, resolve_worker_type_for_profile:470, session_service._resolve_direct_session_worker_profile:784, chat.py:245) filter on status==ARCHIVED. If the unified row isn't synced, archived workers remain visible/dispatchable after read-switch — a real behavior regression.
- **apps/gateway/src/octoagent/gateway/services/control_plane/worker_profile_ops.py** `_save_worker_profile_draft (749-816), _publish_worker_profile_revision (818-864), _sync_worker_profile_agent_profile (157-178), _build_agent_profile_from_worker_profile (109-155)` [high]
  - 现状：Authoring writes worker_profiles (save_worker_profile:786, 852) + worker_profile_revisions (save_worker_profile_revision:841) + then mirrors into agent_profiles via _build/_sync (the Wave 1 builders that copy the 9 fields with profile_id=worker_profile.profile_id, kind=worker, source_kind=worker_profile_mirror metadata).
  - 目标：Wave 2 write-switch: authoring writes the unified agent_profiles row + agent_profile_revisions table DIRECTLY (via save_agent_profile + new save_agent_profile_revision) INSTEAD of worker_profiles/worker_profile_revisions. DELETE the mirror builders _build_agent_profile_from_worker_profile (109-155) and _sync_worker_profile_agent_profile (157-178) — they become redundant once authoring writes the unified row natively. list_worker_profile_revisions (825) → list_agent_profile_revisions; save_worker_profile_revision (841) → save_agent_profile_revision. _save_worker_profile_draft builds a WorkerProfile (787-813) → build/save AgentProfile(kind=worker) instead. NOTE this couples with the read-switch: both must land together or authoring/reading desync.
  - 注：Keep profile_id identity STABLE: the mirror used profile_id=worker_profile.profile_id (same id), so existing dispatch by worker-profile-* id keeps resolving. Do NOT switch to the coordinator's agent-profile-{id} prefix scheme (see coordinator row — that's a pre-existing inconsistency, not Wave 2's to unify). _publish_worker_profile_revision's snapshot/idempotency logic (829-836) and revision-number math (838-840) must be preserved verbatim against the new table.
- **apps/gateway/src/octoagent/gateway/services/control_plane/_coordinator.py** `default-main-agent bootstrap path 969-1010 (worker_profile_id gen + WorkerProfile create 970-984; agent-profile-{worker_profile_id} prefix 987-997; reverse replace 1009-1010, exactly line 1010)` [high]
  - 现状：When project has no default_agent_profile_id: creates worker_profile_id=f'worker-profile-{ULID}', saves a WorkerProfile (971-984), then builds agent_profile_id=f'agent-profile-{worker_profile_id}' (987) — a DIFFERENT prefix from the authoring mirror (which uses same id) — saves a bare AgentProfile (988-996) with NO kind/no worker fields (defaults kind='main'!), then at 1009-1010 reverse-derives worker_profile_id by agent_profile_id.replace('agent-profile-','',1).
  - 目标：Wave 2 must remove the agent-profile-{id} prefix generation + the reverse replace (1009-1010), and create a SINGLE unified agent_profiles row (the main-agent default). Since this is the project's MAIN agent (not a worker), it should keep kind='main' and NOT need the 9 worker fields — but the dual WorkerProfile create (970-984) + bare AgentProfile create (987-997) collapses into one save_agent_profile. The downstream runtime (1021-1037) uses agent_profile_id + worker_profile_id; worker_profile_id feeds AgentRuntime.worker_profile_id (1026) which Wave 4 collapses, so for Wave 2 keep worker_profile_id derivable (set worker_profile_id = agent_profile_id when no longer prefixed) to avoid touching the AgentRuntime dedup (explicitly Wave 4).
  - 注：This is a MAIN-agent path, distinct from the worker authoring mirror — do not conflate. The agent-profile-{worker-profile-id} prefix is a pre-existing id-scheme inconsistency (mirror uses same id, coordinator prepends). Removing the reverse-replace at 1010 must keep AgentRuntime.worker_profile_id populated until Wave 4. Verify no other code parses the 'agent-profile-' prefix off this id.
- **apps/gateway/src/octoagent/gateway/services/capability_pack.py** `resolve_worker_binding (410-430, worker_profile branch), resolve_worker_type_for_profile (469-472), capability_provider_selection reader (1201-1211)` [high]
  - 现状：resolve_worker_binding (the production dispatch read path, called from dispatch_service.py:968) reads get_worker_profile(normalized_profile_id) FIRST (410), gates on status!=ARCHIVED (413), and pulls model_alias/tool_profile/default_tool_groups/selected_tools from the WorkerProfile (422-427); falls back to get_agent_profile (431). resolve_worker_type_for_profile reads worker_profiles (469). The selection reader (1201-1211) reads agent_profile first, worker_profile fallback.
  - 目标：Read-switch: read the unified agent_profiles row (get_agent_profile) and pull the 9 worker fields (model_alias, tool_profile, default_tool_groups, selected_tools, status, etc.) from it; gate is_worker via is_worker_behavior_profile (kind=='worker' OR metadata source_kind). Remove the worker_profiles-first lookup — the unified row now carries all worker fields (Wave 0 absorbed them, Wave 1 populates them). status==ARCHIVED gate now reads agent_profile.status. The 1201-1211 selection reader's worker_profile fallback (1207-1211) becomes dead once the unified row is authoritative — drop it.
  - 注：This is THE coupled read half of Wave 2. Must land atomically with the worker_profile_ops write-switch. profile_id passed here is the worker-profile-* id (stable identity), and get_agent_profile(worker_profile_id) returns the unified row because the Wave 1 mirror used profile_id=worker_profile.profile_id. source_kind=='worker_profile' on the binding is a contract field consumers may key on — verify whether to preserve it.
- **apps/gateway/src/octoagent/gateway/routes/chat.py** `_resolve_explicit_session_owner... (245), _resolve_profile_model_alias (256-258), _resolve_owner_turn_executor_kind (271-275)` [medium]
  - 现状：chat read path probes get_worker_profile to (a) skip legacy_owner if it's a worker (245), (b) resolve model_alias from worker_profile first then agent_profile (256-262), (c) classify TurnExecutorKind.WORKER if a worker_profile exists (271-275).
  - 目标：Switch all three to read the unified agent_profiles row: model_alias from agent_profile (262 already does this as fallback — make it primary), worker-vs-self classification via is_worker_behavior_profile(agent_profile) instead of 'worker_profile exists'. The 245 'is this a worker' check becomes is_worker_behavior_profile on the agent_profile.
  - 注：Part of the chat read consumers the prompt scopes into Wave 2. _resolve_owner_turn_executor_kind currently returns WORKER purely from worker_profiles existence — after switch it must derive from agent_profile.kind=='worker' or it will misclassify every dispatch.
- **apps/gateway/src/octoagent/gateway/services/control_plane/session_service.py** `_resolve_direct_session_worker_profile (779-786); call sites 897 + 961; owner read 512-517` [medium]
  - 现状：Resolves the direct-session entry agent by get_worker_profile(profile_id) (783), gating on status==ARCHIVED (784). The profile_id is request.params['agent_profile_id'] (UI sends an agent-profile id, resolved against worker_profiles). Returns WorkerProfile. Callers (897 session_new, 961 session_create_with_project) only check is-None for availability. A second site (512-517) get_worker_profile of the session owner.
  - 目标：Switch to get_agent_profile(profile_id) returning the unified AgentProfile; gate on agent_profile.status==ARCHIVED. Rename method _resolve_direct_session_worker_profile → _resolve_direct_session_agent_profile and return type WorkerProfile→AgentProfile. Callers only use truthiness, so minimal change beyond the rename. The 512-517 owner get_worker_profile is a second read-switch candidate (reads owner metadata/model_alias). Lines 264/468/606/1004 read runtime.worker_profile_id (AgentRuntime field, Wave 4) — leave.
  - 注：runtime.worker_profile_id reads (264/468/606/1004) are AgentRuntime column, explicitly Wave 4 — do not touch. Confirm 512-517 owner read is in Wave 2 read-switch scope before switching.
- **apps/gateway/src/octoagent/gateway/services/control_plane/worker_service.py** `revisions view (390-411, list_worker_profile_revisions:409) + get_worker_profile sites (394,676,906) + _get_worker_profile_in_scope users (622,626,740,797,855,1007,1083,1128)` [high]
  - 现状：The revisions document handler reads list_worker_profile_revisions(409) + get_worker_profile(394). Apply/clone/archive/publish handlers read get_worker_profile / _get_worker_profile_in_scope, mutate, and write worker_profiles + mirror.
  - 目标：Read path: list_worker_profile_revisions(409)→list_agent_profile_revisions; get_worker_profile reads→get_agent_profile (returning unified AgentProfile, the authoring source post write-switch). _get_worker_profile_in_scope (worker_profile_ops.py:458) returns WorkerProfile→AgentProfile and reads the unified table. These are the authoring-side reads paired with the write-switch — they must read what authoring now writes (agent_profiles).
  - 注：Authoring read+write must be internally consistent: if _save_worker_profile_draft writes agent_profiles but _get_worker_profile_in_scope still reads worker_profiles, update/publish/clone break. Treat the whole worker_service+worker_profile_ops authoring cluster as one atomic switch with capability_pack/chat/session reads.
- **apps/gateway/src/octoagent/gateway/services/agent_context_entity_ensure.py** `_ensure_agent_profile_from_worker_profile (936-1014); callers 836, 868` [medium]
  - 现状：Wave-1 ensure-mirror builder: reads get_worker_profile (942), gates ARCHIVED (943), builds an AgentProfile(kind=worker) with the 9 fields copied + source_kind=worker_profile_mirror metadata (971-1012), saves it (1013). This is the second of the two Wave-1 mirror builders.
  - 目标：After the write-switch makes agent_profiles authoritative, this lazy ensure-from-worker-profile builder becomes redundant for newly-authored profiles. Wave 2 should delete it (parallel to deleting worker_profile_ops mirror builders) IF the authoring path now guarantees the unified row exists. If any read path still lazily materializes the mirror for legacy rows, keep a thin version reading the unified row instead. Confirm callers 836/868 (entity-ensure orchestration) get the unified row directly.
  - 注：Prompt lists 'delete mirror builders' as Wave 2. This is the 2nd builder (1st is worker_profile_ops _build/_sync). Verify the ensure orchestration (831-905) doesn't rely on lazy worker→agent materialization for legacy data before deleting; if it does, that legacy-hydration is a migration_117 concern, not a runtime fallback to keep.
- **apps/gateway/src/octoagent/gateway/services/control_plane/agent_service.py** `paired create path 615-675 (WorkerProfile create 616-630, agent-profile-{worker_profile_id} mirror 633-644, project default_agent_profile_id 664, backfill 671-675)` [high]
  - 现状：Worker-create action builds worker_profile_id=worker-profile-{ULID}, saves WorkerProfile (630), then builds agent_profile_id=f'agent-profile-{worker_profile_id}' (633) — same prefix inconsistency as coordinator — saves AgentProfile(kind=worker) WITHOUT the 9 worker fields (634-644), sets project.default_agent_profile_id=agent_profile_id (664), then double-backfills project_id on both (671-675).
  - 目标：Collapse to a SINGLE save_agent_profile(kind=worker) carrying the 9 worker fields (model_alias, tool_profile, default_tool_groups, selected_tools, runtime_kinds, status, origin_kind, draft/active_revision, summary). Drop the separate WorkerProfile create (616-630) and the agent-profile- prefix (633); use a single profile_id consistent with how dispatch resolves (match the mirror's same-id convention). project.default_agent_profile_id + runtime.agent_profile_id point at the one row. existing_wp get_worker_profile (a sibling site in this file) → get_agent_profile.
  - 注：Second instance of the agent-profile-{worker-profile-id} prefix anti-pattern (coordinator is the other). AgentRuntime.worker_profile_id (697) still set here — Wave 4 collapses it, so keep populated. This create path + coordinator path must use the SAME id-scheme decision so dispatch resolution stays uniform.
  - **gotchas**：agent_profile_revisions TABLE does not exist in sqlite_init.py — it is created ONLY by migration_117 (migration_117_profile_merge.py:474-486). Wave 2 MUST add a byte-identical _AGENT_PROFILE_REVISIONS_DDL to sqlite_init.py CREATE block (~1787) + its index to _AGENT_CONTEXT_INDEXES, or save_agent_profile_revision fails on any un-migrated/fresh DB (all fresh test DBs). / Two DIFFERENT id schemes coexist and must NOT be conflated: (a) the authoring mirror (worker_profile_ops _build_agent_profile_from_worker_profile:131 + agent_context_entity_ensure:972) uses profile_id == worker_profile.profile_id (SAME id), so get_agent_profile(worker-profile-id) returns the unified row; (b) the coordinator (_coordinator.py:987) and agent_service (agent_service.py:633) PREPEND 'agent-profile-' creating agent-profile-{worker-profile-id}, and _coordinator.py:1010 reverse-replaces it. The read-switch relies on scheme (a). Removing scheme (b)'s prefix is in Wave 2 but must keep dispatch resolution uniform. / Archive-sync is the highest-risk gate: worker_service.py _handle_worker_profile_archive (861-869) writes status=ARCHIVED ONLY to worker_profiles and never syncs the unified row (Wave 1 review confirmed stale mirror). EVERY read consumer gates on status==ARCHIVED (capability_pack:413/470, session_service:784, chat:245). After read-switch, an un-synced archive leaves the worker dispatchable — a true behavior regression. Archive must write status/archived_at to the unified agent_profiles row. / Read and write switches are COUPLED and must land atomically: capability_pack.resolve_worker_binding + chat + session reads must switch to agent_profiles in the SAME wave that worker_profile_ops/_save_worker_profile_draft + agent_service stop writing worker_profiles. Authoring-side reads (_get_worker_profile_in_scope:458, worker_service:394/676/906, revisions view:409) must read what authoring now writes, or update/publish/clone desync. / AgentRuntime.worker_profile_id reads (session_service:264/468/606/1004, chat:222/367, dispatch_service, _coordinator:1026, agent_service:697) are explicitly Wave 4 (column dedup collapse) — DO NOT touch in Wave 2. Wave 2 must keep worker_profile_id populated on AgentRuntime even after dropping the agent-profile- prefix (e.g. set it to the unified profile_id). / is_worker_behavior_profile is duplicated in TWO places (agent_decision.py:112 and behavior_workspace/resolver.py:671) — D12 DRY candidate. Both use dual judgment (kind=='worker' OR metadata source_kind=='worker_profile_mirror'/source_worker_profile_id). Wave 2 KEEPS the metadata fallback (live instances have no reliable 'kind' column; mirrors detected via metadata.source_kind). The judgment 收敛/dedup itself is Wave 4 (after worker_profiles tables die) — Wave 2 only switches the READ source, not the detection logic. paths.py:40 resolve_behavior_agent_slug also reads metadata.source_worker_profile_id — leave intact (behavior-slug derivation, not a profile-table read). / WorkerProfileRevision model (agent_context.py:262) + WorkerProfileStatus (80) + WorkerProfileOriginKind (86) are imported by the store (agent_context_store.py:28-31). The 全改名 (WorkerProfile*→AgentProfile*) touches these enum/model names but the store's NEW agent_profile_revision methods reuse the same renamed model. WorkerProfile / WorkerProfileRevision class + worker_profiles/worker_profile_revisions table deletion is Wave 4, NOT Wave 2 — keep save_worker_profile* methods alive through Wave 2. / snapshot_payload idempotency in _publish_worker_profile_revision (worker_profile_ops.py:829-836) compares latest.snapshot_payload + revision + status before writing a new revision. When switching to list_agent_profile_revisions/save_agent_profile_revision, this dedup logic + the revision-number math (838-840) must be preserved verbatim or you get duplicate/skipped revisions.

## 维度：naming rename surface (全改名) — action_id worker_profile.* → agent_profile.* / 6 view classes WorkerProfile* → AgentProfile* / 2 enums WorkerProfileStatus|WorkerProfileOriginKind → AgentProfile* / resource routes worker-profiles → agent-profiles / models re-exports. All file paths relative to octoagent/ (read-only root: /Users/connorlu/Desktop/.workspace2.nosync/OctoAgent/.claude/worktrees/F117-profile-merge/octoagent).

- **apps/gateway/src/octoagent/gateway/services/control_plane/action_registry.py** `lines 330,337,344,351,358,365,372,380 (definition() first arg)` [medium]
  - 现状：8 action definitions with action_id strings worker_profile.create/update/clone/archive/review/apply/publish/bind_default (note: the ~329 line range in the prompt; file lives under gateway/services/control_plane, NOT harness)
  - 目标：rename action_id strings worker_profile.* → agent_profile.* (8 sites). category="root_agents" + 中文 label 不变
  - 注：These 8 string literals are the canonical action registry. MUST be renamed in lock-step with worker_service.py dispatch map (line 77-84) + worker_profile_ops.py/worker_service.py emit sites + FE (Wave 3) which submits these action_ids. worker.spawn_from_profile (387) / worker.extract_profile_from_runtime (393) stay worker.* — NOT in scope.
- **apps/gateway/src/octoagent/gateway/services/control_plane/worker_service.py** `dispatch map lines 77-84; emit sites 374,376,478,480,1001` [high]
  - 现状：_action_handlers dict maps 'worker_profile.review'→_handle_worker_profile_review ... 'worker_profile.bind_default'→_handle_worker_profile_bind_default (8 keys); plus capability_id/action_id literals at 374/376 (create), 478/480 (publish); line 1001 model_copy update action_id='worker_profile.apply'
  - 目标：rename all 8 dispatch-map string KEYS worker_profile.*→agent_profile.* + 5 emit literals. Handler METHOD names _handle_worker_profile_* may stay (internal) or rename for consistency — decide in plan; only the string keys are wire-coupled
  - 注：Dispatch keys must EXACTLY match the renamed action_registry strings or actions 404. Line 1001 internal re-dispatch (apply with publish=True) must use the same renamed string. Adjacent: _handle_worker_profile_archive (848) is the stale-mirror bug from Wave 1 review — Wave 2 read-switch closes it but that's the write/read switch dimension, not naming.
- **apps/gateway/src/octoagent/gateway/services/control_plane/worker_profile_ops.py** `capability_id/action_id literals at 276,278,289,291,301,303,313,315,325,327,341,343,358,360` [medium]
  - 现状：emits ControlPlaneCapability with capability_id/action_id = worker_profile.clone/review/apply/publish/bind_default/clone/archive (14 literal sites across the per-profile capability builder)
  - 目标：rename all 14 capability_id/action_id string literals worker_profile.*→agent_profile.*
  - 注：Pure string literals on the capability surface. Must match renamed action_registry. These feed FE button action dispatch (Wave 3).
- **packages/core/src/octoagent/core/models/control_plane/agent.py** `class defs 39 (WorkerProfileStaticConfig), 51 (WorkerProfileDynamicContext), 70 (WorkerProfileViewItem), 93 (WorkerProfilesDocument), 101 (WorkerProfileRevisionItem), 111 (WorkerProfileRevisionsDocument); resource_type/id literals 94-95,112-113; internal field refs 83-86,97,115; enum import line 10 + field annotations 76-77` [high]
  - 现状：6 view classes named WorkerProfile*. WorkerProfilesDocument.resource_type='worker_profiles' resource_id='worker-profiles:overview' (94-95). WorkerProfileRevisionsDocument.resource_type='worker_profile_revisions' resource_id='worker-profile-revisions:overview' (112-113). Line 10 imports WorkerProfileOriginKind/WorkerProfileStatus; fields at 76-77 annotate origin_kind/status with them. NOTE existing AgentProfileItem (15) + AgentProfilesDocument (32, resource_type='agent_profiles' resource_id='agent-profiles:overview') ALREADY occupy those names.
  - 目标：rename WorkerProfileStaticConfig→AgentProfileStaticConfig, WorkerProfileDynamicContext→AgentProfileDynamicContext, WorkerProfileViewItem→AgentProfileViewItem, WorkerProfileRevisionItem→AgentProfileRevisionItem, WorkerProfileRevisionsDocument→AgentProfileRevisionsDocument — these 5 target names are FREE (verified collision-free). BUT WorkerProfilesDocument CANNOT become AgentProfilesDocument (already exists). resource_type 'worker_profiles'→'agent_profiles' also COLLIDES with existing agent_profiles resource. SEE gotchas — this needs a disambiguation decision before the sweep.
  - 注：4 of 6 classes are clean mechanical rename. WorkerProfilesDocument + resource_type 'worker_profiles' + resource_id 'worker-profiles:overview' are the collision core. Update line-10 enum import + field annotations 76-77 in the same edit (enum dimension overlaps here).
- **apps/gateway/src/octoagent/gateway/services/control_plane/worker_service.py** `imports 48-53; usages 101,131,172,196,259,272,333,392,395,413,443,467; resource_type literals 93-94,604,649,651,716,718,772,774,833,835,880,882,978,980,1061,1063,1108,1189,1253,1255; method names 101,389` [high]
  - 现状：imports + constructs WorkerProfileRevisionItem/RevisionsDocument/ProfilesDocument/StaticConfig/ViewItem; document_routes dict keys 'worker_profiles'(93)/'worker_profile_revisions'(94); ~22 _resource_ref('worker_profiles','worker-profiles:overview') + ('worker_profile_revisions',...) calls; method defs get_worker_profiles_document (101) / get_worker_profile_revisions_document (389)
  - 目标：rename imported class names to AgentProfile* equivalents; rename document_routes string keys worker_profiles→agent_profiles + worker_profile_revisions→agent_profile_revisions; rename ~22 _resource_ref resource_type/id literals; rename the 2 producer method names get_worker_profiles_document→get_agent_profiles_document (COLLIDES — see gotcha) and get_worker_profile_revisions_document→get_agent_profile_revisions_document
  - 注：get_worker_profiles_document→get_agent_profiles_document collides with the EXISTING AgentProfileDomainService.get_agent_profiles_document. Same collision as resource_type. The ~22 _resource_ref literals are the snapshot wire keys the FE reads (resources.worker_profiles) — renaming them is coupled to FE Wave 3, NOT independently safe.
- **apps/gateway/src/octoagent/gateway/services/control_plane/worker_profile_ops.py** `import 35; usages 209,225 (WorkerProfileDynamicContext)` [low]
  - 现状：imports + builds WorkerProfileDynamicContext (the dynamic-context builder)
  - 目标：rename WorkerProfileDynamicContext→AgentProfileDynamicContext (import + 2 usages)
  - 注：Clean mechanical (target name free).
- **apps/gateway/src/octoagent/gateway/routes/control_plane.py** `lines 37-41 (existing agent-profiles route), 44-48 (worker-profiles), 51-58 (worker-profile-revisions)` [high]
  - 现状：@router.get('/api/control/resources/worker-profiles')→control_plane.get_worker_profiles_document() (44-48); @router.get('/api/control/resources/worker-profile-revisions/{profile_id}')→get_worker_profile_revisions_document (51-58). ALREADY EXISTS @router.get('/api/control/resources/agent-profiles')→get_agent_profiles_document (37-41).
  - 目标：rename route paths worker-profiles→agent-profiles and worker-profile-revisions→agent-profile-revisions + the coordinator method calls. BUT /api/control/resources/agent-profiles ALREADY EXISTS (37) → renaming worker-profiles to agent-profiles produces a DUPLICATE FastAPI route + duplicate method name.
  - 注：Hard route-path + method-name collision with pre-existing agent-profiles endpoint. Cannot be a blind sweep. FE api/client.ts (Wave 3) hits these paths — renaming the path breaks FE until Wave 3.
- **apps/gateway/src/octoagent/gateway/services/control_plane/_coordinator.py** `resolvers tuple 652-653 ('agent_profiles' + 'worker_profiles'); lite_sections set ~670-671; delegate methods 797-801` [high]
  - 现状：resolvers register ('agent_profiles', get_agent_profiles_document) AND ('worker_profiles', get_worker_profiles_document) as two distinct snapshot sections; lite_sections lists both; lines 797-801 delegate get_worker_profiles_document / get_worker_profile_revisions_document to _worker_service
  - 目标：rename the 'worker_profiles'/'worker_profile_revisions' resolver+lite_section string keys to agent_profiles*/agent_profile_revisions + delegate method names
  - 注：resolvers dict already has 'agent_profiles' key (line 652) → renaming 'worker_profiles' (653) to 'agent_profiles' produces a DUPLICATE KEY in the snapshot resources dict, silently overwriting one section. This is the structural heart of the resource_type collision.
- **apps/gateway/src/octoagent/gateway/services/control_plane/agent_service.py** `imports 49-50; usages 625-626; resource_ref 382 ('worker_profiles','worker-profiles:overview') + 748 ('worker_profiles','worker_profiles:overview' — note typo colon vs dash)` [medium]
  - 现状：imports WorkerProfileOriginKind/WorkerProfileStatus (49-50), uses at 625-626 (status=ACTIVE, origin_kind=CUSTOM); also emits _resource_ref('worker_profiles',...) at 382 and 748 (748 has resource_id 'worker_profiles:overview' with a colon, inconsistent with the dash form elsewhere)
  - 目标：enum rename WorkerProfileStatus→AgentProfileStatus, WorkerProfileOriginKind→AgentProfileOriginKind (import + usage); resource_type literals at 382/748 worker_profiles→agent_profiles (coupled to resource collision)
  - 注：Enum portion is clean mechanical. The 748 resource_id typo ('worker_profiles:overview' colon, not 'worker-profiles:overview' dash) is a pre-existing inconsistency — flag but don't expand scope.
- **apps/gateway/src/octoagent/gateway/services/control_plane/session_service.py** `import 53; usage 784` [low]
  - 现状：imports WorkerProfileStatus; line 784 'if profile is None or profile.status == WorkerProfileStatus.ARCHIVED'
  - 目标：WorkerProfileStatus→AgentProfileStatus (import + 1 usage)
  - 注：Clean mechanical enum sweep.
- **apps/gateway/src/octoagent/gateway/services/agent_context.py** `import 63` [low]
  - 现状：imports WorkerProfileStatus (re-export/usage in the agent_context service layer)
  - 目标：WorkerProfileStatus→AgentProfileStatus
  - 注：Clean mechanical. Confirm whether it's re-exported downstream.
- **apps/gateway/src/octoagent/gateway/services/agent_context_entity_ensure.py** `import 48; usage 943; (also worker_profile.* field-access lines 994,1010,1011 are NOT action_ids — they read .archived_at/.created_at/.updated_at off a worker_profile var)` [medium]
  - 现状：imports WorkerProfileStatus (48); line 943 status==WorkerProfileStatus.ARCHIVED. The _ensure_agent_profile_from_worker_profile mirror builder (~971) reads worker_profile.archived_at/created_at/updated_at (994/1010/1011)
  - 目标：WorkerProfileStatus→AgentProfileStatus (import + line 943). Lines 994/1010/1011 are local-variable field access, NOT renamed by the action/enum sweep (the var name worker_profile / the mirror builder itself is deleted in Wave 2 read-switch, separate dimension).
  - 注：Don't false-match worker_profile.archived_at as an action_id. The mirror builder _ensure_agent_profile_from_worker_profile gets DELETED in Wave 2 read-switch (other dimension) — coordinate so the enum edit here doesn't conflict with that deletion.
- **apps/gateway/src/octoagent/gateway/services/capability_pack.py** `import 29; usages 413,470; (line 808 'worker_profiles' is a pack-payload dict key — pack.worker_profiles attribute, NOT this resource_type)` [medium]
  - 现状：imports WorkerProfileStatus (29); 413 status!=ARCHIVED, 470 status==ARCHIVED guards; line 808 dumps pack.worker_profiles (a BundledCapabilityPack field, different concept)
  - 目标：WorkerProfileStatus→AgentProfileStatus (import + 413 + 470). Line 808 pack.worker_profiles is OUT OF SCOPE (capability-pack model field, not the control-plane resource_type).
  - 注：capability_pack is the Wave 2 READ-side consumer (reads unified row). The enum rename here is mechanical; do NOT touch pack.worker_profiles (separate BundledCapabilityPack.worker_profiles field — would be its own feature).
- **apps/gateway/src/octoagent/gateway/services/control_plane/_coordinator.py** `enum import 949-950; usages 979-980` [low]
  - 现状：local import of WorkerProfileOriginKind/WorkerProfileStatus inside a method (949-950); status=ACTIVE/origin_kind=CUSTOM at 979-980
  - 目标：WorkerProfileStatus→AgentProfileStatus, WorkerProfileOriginKind→AgentProfileOriginKind
  - 注：Clean mechanical (function-local import).
- **packages/core/src/octoagent/core/store/agent_context_store.py** `imports 29,31; usages 1395,1397,1400,1402,1405,1451,1452` [low]
  - 现状：imports + hydrate logic: WorkerProfileStatus(row['status']) fallback ACTIVE (1395/1397); WorkerProfileOriginKind(row['origin_kind']) fallback CUSTOM (1400/1402/1405); strict construction at 1451/1452
  - 目标：WorkerProfileStatus→AgentProfileStatus, WorkerProfileOriginKind→AgentProfileOriginKind (2 imports + 7 usages)
  - 注：Clean mechanical enum sweep in the store hydrate path. Values ('active'/'custom'/etc) unchanged — StrEnum so DB rows untouched.
- **packages/core/src/octoagent/core/models/agent_context.py** `enum class defs 80 (WorkerProfileStatus), 86 (WorkerProfileOriginKind); field annotations on AgentProfile 228-229, WorkerProfile 253-254, WorkerProfileRevision 262+` [medium]
  - 现状：DEFINES class WorkerProfileStatus(StrEnum) (80-83) + class WorkerProfileOriginKind(StrEnum) (86-90). AgentProfile fields status/origin_kind annotated with them (228-229). WorkerProfile (253-254) + WorkerProfileRevision also use them.
  - 目标：rename the two enum CLASS definitions WorkerProfileStatus→AgentProfileStatus, WorkerProfileOriginKind→AgentProfileOriginKind + all in-file field annotations (228-229,253-254,+WorkerProfileRevision)
  - 注：This is the single DEFINITION site — rename here drives all 11 other files. Field annotations on AgentProfile/WorkerProfile/WorkerProfileRevision must update in same edit. WorkerProfile/WorkerProfileRevision CLASSES themselves stay (Wave 4) — only their enum-typed field annotations change.
- **packages/core/src/octoagent/core/models/__init__.py** `enum re-exports 53,55 (import) + 485-486 (__all__); view-class re-exports 226-231 (import) + 704-709 (__all__)` [high]
  - 现状：re-exports WorkerProfileOriginKind/WorkerProfileStatus (53/55, 485-486) + WorkerProfileDynamicContext/RevisionItem/RevisionsDocument/sDocument/StaticConfig/ViewItem (226-231, 704-709)
  - 目标：rename all enum + 6 view-class re-export names to AgentProfile* equivalents in both the import block and the __all__ list. WorkerProfilesDocument re-export must follow whatever disambiguated name is chosen (cannot be plain AgentProfilesDocument — already exported at 702-703)
  - 注：Public API surface. 5 view classes + 2 enums are clean; WorkerProfilesDocument re-export is the collision (AgentProfilesDocument already in __all__ at 703). Keep alias for Wave 4? No — A1 full merge, but the sDocument name needs resolution.
- **packages/core/src/octoagent/core/models/control_plane/__init__.py** `view-class re-exports 29-34 (import) + 154-159 (__all__)` [high]
  - 现状：re-exports WorkerProfileDynamicContext/RevisionItem/RevisionsDocument/sDocument/StaticConfig/ViewItem (29-34, 154-159)
  - 目标：rename to AgentProfile* equivalents in import block + __all__. WorkerProfilesDocument follows the disambiguation decision.
  - 注：Second public re-export layer. Same WorkerProfilesDocument collision. Note this file does NOT re-export the 2 enums (those come from models.agent_context via models/__init__).
  - **gotchas**：HARD COLLISION (must be resolved before any sweep): there are TWO distinct, pre-existing concepts both wanting the 'AgentProfile' name. (1) Existing AgentProfileItem/AgentProfilesDocument (agent.py:15,32) with resource_type='agent_profiles', resource_id='agent-profiles:overview', route /api/control/resources/agent-profiles (control_plane.py:37), producer AgentProfileDomainService.get_agent_profiles_document, coordinator resolver key 'agent_profiles' (_coordinator.py:652). (2) The WorkerProfilesDocument view (Root Agent capability view) with resource_type='worker_profiles'. Renaming WorkerProfilesDocument→AgentProfilesDocument / resource_type 'worker_profiles'→'agent_profiles' / route worker-profiles→agent-profiles / method get_worker_profiles_document→get_agent_profiles_document / resolver key all DUPLICATE the existing ones → duplicate FastAPI route, duplicate dict keys (snapshot section silently overwritten), duplicate Python method. This is NOT a mechanical sweep — needs a naming decision (e.g. keep WorkerProfilesDocument distinct, or pick AgentCapabilityProfilesDocument / agent_capability_profiles, or merge the two views). Flag to main session for ruling. / WIRE-CONTRACT COUPLING TO WAVE 3 (FE): the resource_type string 'worker_profiles' and resource_id 'worker-profiles:overview' are read DIRECTLY by the frontend — frontend/src/pages/ChatWorkbench.tsx:84 (snapshot.resources.worker_profiles), AgentCenter.tsx:437/491, types/index.ts:747 (resource_type: 'worker_profiles'), App.test.tsx:232-233, plus the route /api/control/resources/worker-profiles in api/client.ts. The action_id strings (worker_profile.review/apply/archive) are also submitted from FE (App.test.tsx, AgentCenter.tsx). Therefore renaming resource_type / resource_id / route path / action_id in Wave 2 backend WITHOUT the FE rename breaks the UI. Either: defer the wire-string renames (resource_type/resource_id/route/action_id) to land atomically WITH Wave 3 FE, OR keep them as-is in Wave 2 and only rename the Python class names + enums + method names (which are internal). Recommend: Wave 2 = internal symbol renames (classes, enums, handler methods) only; wire strings move in Wave 3. / CLEAN MECHANICAL (low risk, safe in Wave 2): the 2 enums WorkerProfileStatus→AgentProfileStatus + WorkerProfileOriginKind→AgentProfileOriginKind across all 12 files (defined agent_context.py:80,86; consumed in session_service/agent_context/worker_profile_ops/worker_service/agent_service/capability_pack/_coordinator/agent_context_entity_ensure/agent_context_store + control_plane/agent.py + the 2 model __init__ re-exports). Target names verified collision-free. StrEnum values unchanged → zero DB/wire impact. AND 5 of the 6 view classes (StaticConfig/DynamicContext/ViewItem/RevisionItem/RevisionsDocument) → AgentProfile* are collision-free internal renames. / FALSE-MATCH TRAPS in the 'worker_profile' text sweep (do NOT rename these): (a) agent_context_entity_ensure.py:994/1010/1011 worker_profile.archived_at/created_at/updated_at = local var field access, not action_ids; the mirror builder is deleted by Wave 2 read-switch. (b) capability_pack.py:808 + session_tools.py:125 'worker_profiles' = BundledCapabilityPack.worker_profiles model field, a different concept (capability pack payload), out of scope. (c) AgentRuntime.worker_profile_id = Wave 4 dedup, not naming. (d) WorkerProfile / WorkerProfileRevision MODEL classes (agent_context.py:238,262) + worker_profiles / worker_profile_revisions TABLES = Wave 4 deletion, only their enum-typed field annotations change in Wave 2. (e) agent_service.py:748 resource_id 'worker_profiles:overview' uses a colon (typo) vs the dash 'worker-profiles:overview' everywhere else — pre-existing inconsistency, flag don't fix. / DISPATCH-KEY EXACT-MATCH INVARIANT: the action_registry.py definition() strings (330-385), the worker_service.py _action_handlers dispatch map keys (77-84), the worker_service.py:1001 internal re-dispatch (model_copy action_id), and the worker_profile_ops.py/worker_service.py capability_id/action_id emit literals must ALL rename in lock-step — any drift makes the action 404 at dispatch. Handler METHOD names (_handle_worker_profile_*) are internal and can stay or rename independently; only the string keys are wire-coupled. / COORDINATOR DUPLICATE-KEY HAZARD: _coordinator.py builds the snapshot resources dict from the resolvers tuple (652-653) which already contains BOTH 'agent_profiles' and 'worker_profiles' as separate entries, and lite_sections (670-671) lists both. Renaming 'worker_profiles'→'agent_profiles' there creates two tuple entries / set members with the same key → the second resolver silently overwrites the first section in the resulting dict (no error, data loss). This is the runtime manifestation of the resource_type collision and the strongest argument for keeping the wire key distinct.

## 维度：backend tests + cross-checks (F117 Wave 2: A1 full merge + 全改名)

- **apps/gateway/tests/test_control_plane_api.py** `WIRE CONTRACT block — action_id strings: line 5126 "worker_profile.review", 5146 "worker_profile.create", 5179 "worker_profile.publish", 5213 "worker_profile.bind_default", 5258 "worker_profile.review", 5301 "worker_profile.create", 5345 "worker_profile.create". Routes: GET "/api/control/resources/worker-profiles" at 761, 5164, 5229, 5311, 5355, 5655, 5732. Revisions route 5201 "/api/control/resources/worker-profile-revisions/{profile_id}". resource_type assertion 788: worker_profiles_payload["resource_type"] == "worker_profiles"` [high]
  - 现状：Asserts the WIRE contract: posts action_id="worker_profile.*" to /api/control/actions, GETs /api/control/resources/worker-profiles, and asserts resource_type=="worker_profiles". These are over-the-wire HTTP strings — when production renames action_ids/routes/resource_type, these tests MUST change or they 404/fail.
  - 目标：Rename every action_id string "worker_profile.review|create|publish|bind_default" → "agent_profile.*"; route "/api/control/resources/worker-profiles" → "agent-profiles"; revisions route "worker-profile-revisions" → "agent-profile-revisions"; resource_type assertion "worker_profiles" → "agent_profiles". Match exactly whatever new wire names the Wave 2 production action registry + control_plane route table emit.
  - 注：MUST change — these are the only WIRE-contract assertions in the whole backend test suite. If production renames action_ids but these strings stay, the action registry returns ACTION_NOT_FOUND and 7 tests fail. Verify the EXACT new action_id casing against production worker_actions registry (review/create/publish/bind_default confirmed in tests; production also has apply/archive/clone/update — those have no test wire assertion). Also line 5430/5434/5437 assert control_metadata['requested_worker_profile_id'] + 'requested_worker_profile_version' + 'effective_worker_snapshot_id' — these are envelope metadata keys; only rename if Wave 2 renames those metadata keys (likely NOT — they're delegation envelope keys, defer with Work field).
- **apps/gateway/tests/test_control_plane_api.py** `MODEL/ENUM imports line 44-47 (WorkerProfile, WorkerProfileOriginKind, WorkerProfileRevision, WorkerProfileStatus); construction sites status=WorkerProfileStatus.ACTIVE at 3714,3758,3880,3960,4027,4107,4179,4289,5389,5515,5698; origin_kind=WorkerProfileOriginKind.{CUSTOM,EXTRACTED} at 5390,5489,5516,5699; WorkerProfile(...) at 5378,5504,5687; WorkerProfileRevision(...) at 5396,5522; store calls save_worker_profile/get_worker_profile/save_worker_profile_revision at 3707,3751,3872,3952,4019,4099,4171,4281,5394,5395,5487,5520,5521,5703` [high]
  - 现状：Imports WorkerProfileStatus/WorkerProfileOriginKind/WorkerProfileRevision enums+model and constructs WorkerProfile(status=WorkerProfileStatus.ACTIVE, origin_kind=WorkerProfileOriginKind.CUSTOM). Also calls agent_context_store.save_worker_profile / get_worker_profile / save_worker_profile_revision.
  - 目标：Rename enum imports+usages WorkerProfileStatus→AgentProfileStatus, WorkerProfileOriginKind→AgentProfileOriginKind (Wave 2 enum rename). WorkerProfile + WorkerProfileRevision model classes STAY (Wave 4) but their status/origin_kind field types now reference the renamed enums so the .ACTIVE/.CUSTOM/.EXTRACTED member access must use the new enum name. If Wave 2 switches authoring to save the unified agent_profiles row, store calls save_worker_profile→(new unified writer name) / get_worker_profile→get_agent_profile / save_worker_profile_revision→(new revisions writer) — confirm exact new store method names from Wave 2 worker_service/worker_profile_ops switch.
  - 注：These are MODEL-object tests (not wire) EXCEPT the enum member access breaks at import time if enums rename. WorkerProfile class itself is Wave 4, so keep the class name but rename the two enum imports. Store method rename is the riskier coupling: Wave 2 deletes mirror builders + switches authoring to unified agent_profiles+agent_profile_revisions — if save_worker_profile is retired the tests calling it break. Confirm whether Wave 2 keeps save_worker_profile as a thin alias or fully renames.
- **apps/gateway/tests/test_task_service_context_integration.py** `imports 31-33 (WorkerProfile, WorkerProfileOriginKind, WorkerProfileStatus); 18 construction/enum sites: status=WorkerProfileStatus.ACTIVE & origin_kind=WorkerProfileOriginKind.{CUSTOM,BUILTIN} at 287-288,1065-1066,1286-1287,1466-1467,1635-1636,1786-1787,3056-3057,3218-3219,3360-3361,3429-3430,3530-3531; save_worker_profile calls at 279,1070,1291,1471,1640,1791,3060,3210,3352,3421,3522` [high]
  - 现状：Largest model-object consumer (36 WorkerProfile occurrences). Imports the two enums + WorkerProfile model; constructs WorkerProfile rows with WorkerProfileStatus/WorkerProfileOriginKind; saves via save_worker_profile. Also references worker_profile_mirror metadata + is_worker_behavior_profile detection path.
  - 目标：Same as control_plane: rename enum imports/usages WorkerProfileStatus→AgentProfileStatus, WorkerProfileOriginKind→AgentProfileOriginKind; keep WorkerProfile class (Wave 4); rename save_worker_profile store call to the Wave-2 unified writer name. Leave metadata source_kind='worker_profile_mirror' + is_worker_behavior_profile detection UNTOUCHED (KEY FACT a — mirrors still detected by metadata until Wave 4).
  - 注：NOT wire contract — pure model/store object usage. 62 snake_case worker_profile hits include is_worker_behavior_profile/worker_profile_mirror detection assertions that MUST survive Wave 2 (live instances have no kind column). Distinguish those from save_worker_profile store renames. High volume → mechanical sweep but verify each enum member access.
- **packages/core/tests/test_agent_context_store.py** `imports 25-28 (WorkerProfile, WorkerProfileOriginKind, WorkerProfileRevision, WorkerProfileStatus); WorkerProfile(...) 277; status=WorkerProfileStatus.ACTIVE 288; origin_kind=WorkerProfileOriginKind.CUSTOM 289; WorkerProfileRevision(...) 293; store roundtrip save_worker_profile 306, save_worker_profile_revision 307, get_worker_profile 310, list_worker_profile_revisions 311; assert stored_profile.origin_kind==WorkerProfileOriginKind.CUSTOM 318` [high]
  - 现状：Store-layer roundtrip test: saves WorkerProfile + WorkerProfileRevision, reads back via get_worker_profile / list_worker_profile_revisions, asserts origin_kind enum roundtrip.
  - 目标：Rename enum imports WorkerProfileStatus→AgentProfileStatus, WorkerProfileOriginKind→AgentProfileOriginKind. WorkerProfile/WorkerProfileRevision classes stay (Wave 4). Rename store methods to Wave-2 unified API: save_worker_profile→save unified agent_profiles; save_worker_profile_revision→save agent_profile_revisions; get_worker_profile→get_agent_profile; list_worker_profile_revisions→list_agent_profile_revisions. Confirm exact names from agent_context_store.py Wave 2 switch.
  - 注：This is THE store-contract test that exercises the new agent_profile_revisions table. After Wave 2 read-switch it should roundtrip through the unified table. If Wave 2 keeps WorkerProfile model but persists to agent_profiles+agent_profile_revisions, this test validates the migration_117 DDL match (see gotcha on revisions DDL).
- **apps/gateway/tests/test_orchestrator.py** `imports 27-29 (WorkerProfile, WorkerProfileOriginKind, WorkerProfileStatus); worker_profile=WorkerProfile(...) 295 & 368; status=WorkerProfileStatus.ACTIVE 304,377; origin_kind=WorkerProfileOriginKind.CUSTOM 305,378; save_worker_profile 309,382` [medium]
  - 现状：Constructs two WorkerProfile fixtures with the two enums, saves via save_worker_profile. Local var named worker_profile.
  - 目标：Rename enum imports/usages →AgentProfileStatus/AgentProfileOriginKind; keep WorkerProfile class (Wave 4); rename save_worker_profile store call to Wave-2 writer. Local var rename worker_profile→agent_profile optional (cosmetic 全改名).
  - 注：NOT wire. Enum rename mandatory (breaks at import). Store method rename depends on Wave 2 keeping/renaming save_worker_profile.
- **apps/gateway/tests/test_delegation_plane.py** `imports 17-19 (WorkerProfile, WorkerProfileOriginKind, WorkerProfileStatus); profile=WorkerProfile(...) 188; status=WorkerProfileStatus.ACTIVE 199; origin_kind=WorkerProfileOriginKind.CUSTOM 200; save_worker_profile 204` [medium]
  - 现状：Single WorkerProfile fixture + save_worker_profile.
  - 目标：Rename enum imports/usages →AgentProfile*; keep WorkerProfile class; rename save_worker_profile store call.
  - 注：NOT wire. Mechanical.
- **apps/gateway/tests/test_chat_send_route.py** `imports 19-20 (WorkerProfile, WorkerProfileStatus — note: NO OriginKind here); WorkerProfile(...) 84,357; status=WorkerProfileStatus.ACTIVE 90,363; save_worker_profile 83,356` [medium]
  - 现状：Two WorkerProfile fixtures using only WorkerProfileStatus (no OriginKind import); save_worker_profile.
  - 目标：Rename WorkerProfileStatus→AgentProfileStatus import+usages; keep WorkerProfile class; rename save_worker_profile store call.
  - 注：NOT wire. Only the Status enum is imported here (smaller delta than other files).
- **apps/gateway/tests/test_f105_conversation_binding.py** `import 17: `from octoagent.core.models import WorkerProfile, WorkerProfileStatus`; status=WorkerProfileStatus.ACTIVE 235; save_worker_profile 228` [low]
  - 现状：Single-line import of WorkerProfile + WorkerProfileStatus; one fixture; save_worker_profile.
  - 目标：Rename WorkerProfileStatus→AgentProfileStatus on the single import line + line 235; keep WorkerProfile; rename save_worker_profile store call.
  - 注：NOT wire. Smallest delta — single import line, single enum usage. The `from ... import Project` at line 195 is unrelated.
- **packages/core/tests/test_work_store.py** `requested_worker_profile_id="worker-profile-alpha" 65; requested_worker_profile_version=2 66; assert stored.requested_worker_profile_id==... 88; assert stored.requested_worker_profile_version==2 89` [high]
  - 现状：Asserts Work model field requested_worker_profile_id / requested_worker_profile_version roundtrip through work_store (the DB column migration_117 RENAMES to requested_agent_profile_id/version).
  - 目标：DECISION-DEPENDENT. The prompt's Wave 2 explicit list does NOT name the Work field rename, but migration_117 §6 renames the works column requested_worker_profile_id→requested_agent_profile_id. If Wave 2 couples the column rename (so work_store SQL + Work.requested_worker_profile_id Pydantic field must rename together), then update these 4 lines to requested_agent_profile_id/requested_agent_profile_version. If the Work field rename is deferred (Wave 4 with the AgentRuntime dedup), leave UNTOUCHED.
  - 注：GOTCHA — see gotchas[]. This is the single biggest scope ambiguity: migration renames the works column but the prompt's Wave 2 list omits the Work field. work_store.py:27/51/84/359 + delegation.py:278 + control_plane/work.py:33 all use requested_worker_profile_id — if the migration column rename lands without the field/SQL rename, work_store breaks at runtime. Must be resolved before Wave 2.
- **apps/gateway/tests/services/test_phase_b_a2a_source_target.py** `requested_worker_profile_id= used as method kwarg to _resolve_target_agent_profile/resolve_worker_binding at 233,271,311,345,368,403 (plus docstring 10,211,290)` [medium]
  - 现状：Passes requested_worker_profile_id= as a keyword arg to the resolver method (NOT a Work model field here — it is the resolver's parameter name).
  - 目标：Only rename if Wave 2 renames the resolver method PARAMETER requested_worker_profile_id→requested_agent_profile_id. Independent of the Work column rename. Likely defer unless the 全改名 sweep covers resolver param names.
  - 注：Method-parameter name, not a model field or wire string. Lower priority than the Work field decision. Verify against the Wave 2 production resolver signature (_resolve_target_agent_profile).
- **apps/gateway/tests/services/test_phase_i_worker_audit_chain.py** `requested_worker_profile_id="profile-target-research-worker" 77 (kwarg to _resolve_target_agent_profile per line 76)` [medium]
  - 现状：Passes requested_worker_profile_id= as resolver method kwarg.
  - 目标：Same as test_phase_b — rename only if resolver param renames.
  - 注：Resolver param, not Work field. Couple with test_phase_b decision.
- **packages/core/tests/test_behavior_workspace.py** `45 snake_case worker_profile hits: metadata source_kind='worker_profile_mirror' 48,92; source_worker_profile_id 49,93; is_worker_profile=True/False param 348,373,389,554,576,...; test method names test_worker_profile_includes_8_files 281 / test_worker_profile_excludes_bootstrap_only 291 / test_worker_profile_returns_subset 334; helper _make_worker_profile()->AgentProfile 42` [low]
  - 现状：Uses worker_profile in THREE distinct senses: (1) metadata.source_kind='worker_profile_mirror' + source_worker_profile_id — the worker-detection markers; (2) is_worker_profile= behavior-materialization param (SOUL.worker.md/HEARTBEAT.worker.md variant selection); (3) test-method names + local helper describing worker behavior allowlist.
  - 目标：DO NOT rename in Wave 2. The metadata markers source_kind='worker_profile_mirror' + source_worker_profile_id are KEY FACT (a) — live instances detect worker mirrors ONLY via these (no kind column); is_worker_behavior_profile depends on them. is_worker_profile= is a behavior concept, not the profile-merge rename. Test method/helper names are cosmetic.
  - 注：FALSE POSITIVE for Wave 2 — appears in the snake_case grep (45 hits, highest count) but is NOT wire/enum/view/route/store-method. Renaming worker_profile_mirror would BREAK migration_117 detection (_detect_worker_mirror_profile_ids) and Wave-1 mirror builders' metadata. Explicitly exclude.
- **apps/gateway/tests/services/test_agent_decision_envelope.py** `worker_profile local var holding AgentProfile(kind="worker") 243,549,688; is_worker_profile= param 529,552,692; agent_profile=worker_profile 249,557,566,587,695` [low]
  - 现状：Uses worker_profile as a LOCAL VARIABLE NAME for an AgentProfile(kind='worker') and is_worker_profile= behavior param.
  - 目标：DO NOT rename for correctness (no wire/enum/view/route). Local var rename worker_profile→agent_profile only if the 全改名 sweep insists on cosmetic local-var consistency; no functional change.
  - 注：FALSE POSITIVE for functional Wave 2. AgentProfile already used; only the local identifier says 'worker'.
- **apps/gateway/tests/services/{test_phase_f_ephemeral_runtime.py, test_phase_g..., test_agent_context_phase_c.py, tools/test_graph_pipeline_security.py}** `test_phase_f_ephemeral_runtime.py (6 snake_case + worker_profile_mirror detection), test_behavior_pack_loaded_phase_g.py (6), test_agent_context_phase_c.py (3), tools/test_graph_pipeline_security.py (1)` [low]
  - 现状：Mix of worker_profile_mirror detection metadata + local var names + is_worker_profile behavior param. None reference WorkerProfile class, the renamed enums, action_ids, routes, or view classes.
  - 目标：DO NOT rename in Wave 2 except where they call save_worker_profile/get_worker_profile store methods or import the renamed enums (none detected in class/enum grep). Leave worker_profile_mirror detection intact.
  - 注：Low-priority false positives. test_phase_f has worker_profile_mirror detection (must survive). Verify none import WorkerProfileStatus/OriginKind before touching.
- **apps/gateway/tests/test_worker_runtime.py + tests/integration/test_f009_worker_runtime_flow.py** `test_worker_runtime.py:118 assert error_type=="WorkerProfileDeniedError"; test_f009_worker_runtime_flow.py:193 same string` [low]
  - 现状：Assert error_type string "WorkerProfileDeniedError" (production class worker_runtime.py:51 class WorkerProfileDeniedError).
  - 目标：Rename only if Wave 2 全改名 renames the production exception class WorkerProfileDeniedError→AgentProfileDeniedError. The error_type is the class __name__, so the string assertion follows the class rename. If the exception class is out of the prompt's named Wave 2 scope (it is not listed), leave UNTOUCHED.
  - 注：FALSE POSITIVE for the named Wave 2 scope. WorkerProfileDeniedError is a worker_runtime exception, not in the prompt's rename list (action_id/view/enum/route). Flag as 'verify whether 全改名 includes exception classes'. If kept, no test change.
- **packages/skills/tests/test_limits_merge.py** `line 106 docstring: "...global_defaults -> AgentProfile -> WorkerProfile -> SKILL.md"` [low]
  - 现状：Docstring text only mentions WorkerProfile in a priority-chain comment.
  - 目标：Optional doc text update WorkerProfile→AgentProfile in the docstring for accuracy after merge. No code change.
  - 注：Pure docstring. Cosmetic. No import, no class usage.
- **apps/gateway/tests/{test_capability_pack_tools.py, test_bootstrap_simplification.py}** `capability_pack.get_worker_profile("general"/"dev"/"research"/"ops") at test_capability_pack_tools.py:214,2081,2083,2084,2085 and test_bootstrap_simplification.py:233,235` [low]
  - 现状：Calls capability_pack.get_worker_profile(worker_type) which returns WorkerCapabilityProfile (capability_pack.py:333) — a DIFFERENT type from the store WorkerProfile model.
  - 目标：DO NOT rename in Wave 2. This is the builtin worker_type capability bundle (general/dev/research/ops), NOT the merged WorkerProfile entity. Out of F117 scope entirely.
  - 注：FALSE POSITIVE — same method name get_worker_profile but on capability_pack (returns WorkerCapabilityProfile, not the store model). Explicitly exclude; renaming would be wrong (these are capability concepts, not profile rows).
- **frontend/src/** (16 files) — Wave 3, DO NOT touch in Wave 2** `App.test.tsx, api/client.ts, components/shell/WorkbenchLayout.tsx+.test, domains/agents/agentManagementData.ts, domains/settings/SettingsPage.tsx+.test+SettingsResourceLimitsSection.tsx, pages/AgentCenter.tsx+.test, pages/ChatWorkbench.tsx+.test, platform/contracts/controlPlane.ts+controlPlaneContract.test.ts, platform/queries/controlPlaneResources.test.ts, types/index.ts. FE wire names: AgentCenter.tsx action_ids worker_profile.{review,apply,archive} (183,564,576,590,620,632,633); controlPlaneContract.test.ts endpointPath '/api/control/resources/worker-profiles' + route 'worker-profiles' + snapshotKey 'worker_profiles' (38,40,41); controlPlaneResources.test.ts resource_type 'worker_profiles' (99,100,146)` [medium]
  - 现状：FE (TS/Vitest) references the worker_profile wire contract (action_ids, route, resource_type, snapshotKey) and worker_profile types. The FE contract tests (controlPlaneContract.test.ts, controlPlaneResources.test.ts, App.test.tsx, AgentCenter.test.tsx) assert the OLD wire names.
  - 目标：DO NOT touch in Wave 2 (FE rename = Wave 3). These break the FE at runtime once the backend wire contract renames (route 404 / action ACTION_NOT_FOUND), and the FE Vitest contract tests will fail — but that is EXPECTED until Wave 3 catches up.
  - 注：CROSS-CHECK ANSWER: confirmed FE is Wave 3. CRITICAL FINDING for the prompt's question: NO backend Python test cross-checks the FE worker_profile contract — the FE contract tests are TS/Vitest-only (no pytest imports/validates a shared OpenAPI or contract.json for worker_profiles; grep of openapi/frontend Python tests found only frontend-version-endpoint tests, 0 worker_profile refs). So Wave 2 backend wire renames will NOT trip any backend pytest via FE; they WILL break FE Vitest contract tests, which are deferred to Wave 3. Sequencing risk: if Wave 2 ships before Wave 3, the running app's FE breaks (route/action mismatch) until Wave 3.
- **packages/core/src/octoagent/core/store/sqlite_init.py** `agent_profiles DDL 382-413 (already has F117 9 cols per comment 398); worker_profiles DDL 414-433; worker_profile_revisions DDL 436-448 (FK→worker_profiles, UNIQUE(profile_id,revision)); index idx_worker_profiles_scope_project 1020-1021; idx_worker_profile_revisions_profile_created 1024-1025` [high]
  - 现状：sqlite_init.py creates worker_profiles + worker_profile_revisions tables for FRESH DBs. It does NOT yet create agent_profile_revisions. migration_117 creates agent_profile_revisions (lines 475-487) only on apply.
  - 目标：Add agent_profile_revisions DDL to sqlite_init.py matching migration_117 EXACTLY: revision_id TEXT PRIMARY KEY, profile_id TEXT NOT NULL, revision INTEGER NOT NULL, change_summary TEXT NOT NULL DEFAULT '', snapshot_payload TEXT NOT NULL DEFAULT '{}', created_by TEXT NOT NULL DEFAULT '', created_at TEXT NOT NULL, FOREIGN KEY(profile_id) REFERENCES agent_profiles(profile_id), UNIQUE(profile_id, revision). Add matching index. Once Wave 2 stops authoring worker_profiles, DROP the worker_profiles + worker_profile_revisions CREATE blocks + their two indexes from fresh-DB init (or keep until Wave 4 — see gotcha).
  - 注：GOTCHA — see gotchas[]. The agent_profile_revisions DDL in migration_117 has IDENTICAL columns to worker_profile_revisions but FK→agent_profiles. Fresh DBs created by sqlite_init must get agent_profile_revisions so test_agent_context_store.py roundtrip works without running migration_117. Mismatched DDL (e.g. a column default drift) between sqlite_init and migration_117 = fresh-DB tests pass but migrated instances differ.
  - **gotchas**：WIRE vs MODEL split (the prompt's core ask): Exactly ONE backend test file asserts the WIRE contract — apps/gateway/tests/test_control_plane_api.py. The wire strings are 7 action_ids (worker_profile.review/create/publish/bind_default at lines 5126/5146/5179/5213/5258/5301/5345), 7 route GETs (worker-profiles at 761/5164/5229/5311/5355/5655/5732), 1 revisions route (worker-profile-revisions 5201), and 1 resource_type assertion (worker_profiles 788). ALL other ~10 test files use WorkerProfile/enums purely as MODEL OBJECTS (constructor + store save) — those break only at import-time when the enums rename, not over the wire. / Work column rename is the #1 scope ambiguity. migration_117 §6 (lines 519-535) RENAMES works.requested_worker_profile_id→requested_agent_profile_id AND requested_worker_profile_version→requested_agent_profile_version. But the prompt's Wave 2 explicit list does NOT mention the Work Pydantic field or work_store SQL. work_store.py:27/51/84/359 + delegation.py:278 + control_plane/work.py:33 all use the old name. If migration apply lands in Wave 2 without coupling the field+SQL rename, work_store INSERT/SELECT breaks (column gone). test_work_store.py:65/66/88/89 assert the old field. MUST decide: couple Work field rename into Wave 2 (then update test_work_store.py) OR defer the works column rename to Wave 4. This is a hard gate before Wave 2. / agent_profile_revisions DDL parity: migration_117 creates agent_profile_revisions (lines 475-487) but sqlite_init.py does NOT — it only creates worker_profile_revisions (436-448). Wave 2 must add agent_profile_revisions to sqlite_init with BYTE-IDENTICAL columns to the migration (revision_id PK, profile_id, revision, change_summary DEFAULT '', snapshot_payload DEFAULT '{}', created_by DEFAULT '', created_at, FK→agent_profiles, UNIQUE(profile_id,revision)). Otherwise fresh-DB pytest (test_agent_context_store.py roundtrip) and migrated-instance schemas diverge. / FALSE POSITIVES in the snake_case grep (do NOT rename in Wave 2): (1) metadata source_kind='worker_profile_mirror' + source_worker_profile_id (test_behavior_workspace.py, test_task_service_context_integration.py, test_phase_f) — KEY FACT (a) worker-detection markers; renaming breaks migration_117 _detect_worker_mirror_profile_ids + is_worker_behavior_profile on live no-kind-column instances. (2) capability_pack.get_worker_profile() (test_capability_pack_tools.py, test_bootstrap_simplification.py) — returns WorkerCapabilityProfile, a DIFFERENT type (builtin worker_type bundle), out of F117 scope. (3) is_worker_profile= behavior-materialization param — behavior concept, not the merge. (4) WorkerProfileDeniedError error_type (test_worker_runtime.py:118, test_f009:193) — exception class not in named Wave 2 scope. (5) local var names worker_profile (test_agent_decision_envelope.py). / NO backend test cross-checks the FE contract. The FE (16 files under frontend/, Wave 3) has its own TS/Vitest contract tests (controlPlaneContract.test.ts asserts route 'worker-profiles'+snapshotKey 'worker_profiles'; controlPlaneResources.test.ts asserts resource_type 'worker_profiles'; App.test.tsx + AgentCenter.test.tsx post action_id 'worker_profile.*'). A grep for openapi/contract/frontend Python tests returned only frontend-VERSION-endpoint tests with 0 worker_profile refs. So Wave 2 backend wire renames will NOT fail any pytest via FE — but they WILL break the FE Vitest contract tests and the running FE app (route 404 / action not found) until Wave 3. Sequencing: ship Wave 2 + Wave 3 close together, or the app's Agents page breaks in between. / NO e2e_smoke / e2e_live test touches worker profiles. The entire apps/gateway/tests/e2e_live/ suite (test_e2e_*.py incl. delegation_a2a, safety_gates, smoke_real_llm) has ZERO refs to worker_profile / WorkerProfile / worker-profiles / action_ids. e2e_command.py CLI also has none. So Wave 2 needs no e2e test edits and the pre-commit e2e_smoke hook won't catch wire-contract drift — rely on test_control_plane_api.py for that. / Enum rename touches the KEPT WorkerProfile model. The WorkerProfile class (agent_context.py:238, stays until Wave 4) declares fields status: WorkerProfileStatus = WorkerProfileStatus.DRAFT (line 253) and origin_kind: WorkerProfileOriginKind = WorkerProfileOriginKind.CUSTOM (254). When Wave 2 renames the enums, the kept model's field annotations + defaults must update to AgentProfileStatus/AgentProfileOriginKind even though the class name stays WorkerProfile. Every test constructing WorkerProfile(status=WorkerProfileStatus.ACTIVE, origin_kind=WorkerProfileOriginKind.CUSTOM) (8 model-object test files) then needs the enum member access renamed. Consider whether Wave 2 keeps WorkerProfileStatus/WorkerProfileOriginKind as backward-compat aliases (= AgentProfileStatus) to shrink the test delta — the prompt's enum-rename line suggests NO alias (hard rename). / Sizing: ~270 occurrence rewrite confirmed test-side. Backend test occurrence counts — WorkerProfile* (CamelCase, class/enum/view): test_task_service_context_integration.py 36, test_control_plane_api.py 32, then 9/9/6/6/3/1/1/1. worker_profile (snake: action_id/route/field/store/detection): test_control_plane_api.py 79, test_task_service_context_integration.py 62, test_behavior_workspace.py 45 (mostly false-positive detection markers), then 16/15/13/10/10/9/8/6/6/6/4/3/2/2/1/1. The 45 in test_behavior_workspace are predominantly worker_profile_mirror/is_worker_profile false positives — net true Wave-2 test edits concentrate in test_control_plane_api.py (wire+model) and test_task_service_context_integration.py (model+store).

---

## 完整性 critic（GAP + HAZARD + 子波序）

The store-call file list confirms exactly 10 production files have worker_profiles store calls. Of these, **`dispatch_service.py` and `session_projection_helpers.py` are NOT explicitly mapped as read-switch targets** (dispatch_service only appears as a *caller* of `resolve_worker_binding` in the maps, never for its own `get_worker_profile:686`). `agent_service.py:368` (resource_limits handler) is only mentioned in passing.

I now have a complete, verified picture. Here is my critique.

---

# F117 Wave 2 Change Map — Completeness Critique

## VERDICT
The 6 maps are **strong on the named scope but have 3 unmapped production read sites, 1 unmapped authoring read+write site, and 1 unresolved hard contradiction (the Work-column rename) that will break runtime if Wave 2 lands as currently bounded.** Sequencing guidance below.

---

## (1) GAPS — sites/behaviors NOT covered by any map

**GAP-A [HIGH] — `dispatch_service.py:686` `get_worker_profile` is unmapped.** `_build_agent_runtime` (the hot dispatch path) reads `get_worker_profile(worker_profile_id)` to populate `AgentRuntime.name` / `persona_summary` (line 697/702) and `resolve_permission_preset(worker_profile, agent_profile)` (line 714). Maps reference `dispatch_service.py:968` only as the *caller* of `resolve_worker_binding`, never this independent reader. Must switch to `get_agent_profile` in Wave 2 — the row carries `name`/`summary`/`metadata` (Wave 0/1). `resolve_permission_preset` (agent_context.py:53) is `*profiles: Any` variadic, so passing an `AgentProfile` instead of `WorkerProfile` is type-safe (reads `metadata['permission_preset']`).

**GAP-B [HIGH] — `agent_context_entity_ensure.py:218` `get_worker_profile` is unmapped.** The mapped site is `942` (`_ensure_agent_profile_from_worker_profile`, the mirror builder to delete). But line 218 is a *separate* reader inside the runtime-ensure path (sibling to dispatch_service:686, same `name`/`summary` population at 227/233). Deleting the 942 mirror builder does NOT touch 218 — it must independently switch to `get_agent_profile` or it remains a residual `worker_profiles` reader that Wave 4 (table drop) breaks.

**GAP-C [HIGH] — `session_projection_helpers.py:95 + 107` two `get_worker_profile` readers unmapped.** `_resolve_profile_display_name` (95) is already `worker_profile`-first / `agent_profile`-fallback (identical pattern to the chat.py helpers); `_is_worker_profile_id` (107) uses *presence in worker_profiles* as the worker-detection signal (identical to chat.py:271's anti-pattern). Both must switch: 95 → collapse to single `get_agent_profile` read; 107 → `get_agent_profile` + `is_worker_behavior_profile`. **This file is never named in any of the 6 maps.** It feeds session-projection compatibility-flag logic (legacy worker detection at 130-176), so misclassification here corrupts session owner/delegation-target resolution.

**GAP-D [MEDIUM] — `agent_service.py:367-380 `_handle_update_resource_limits` (target_type="worker_profile") read+write unmapped.** Dimension 4's map covers `agent_service.py:615-675` (the create path) and mentions "existing_wp get_worker_profile (a sibling site)" in passing, but never gives the `resource_limits.update` handler a change entry. It does `get_worker_profile(368)` + `model_copy` + `save_worker_profile(380)` + `_resource_ref("worker_profiles", "worker-profiles:overview")(382)`. Must switch reads/writes to the unified row. **Non-regression note I verified:** both `save_worker_profile` AND `save_agent_profile` deliberately do NOT persist `resource_limits` (it's a F117-out-of-scope dead column on both sides — confirmed agent_context_store.py:100 comment + worker save block 156-171). So this handler is *already* a no-op for the actual limit value; switching stores preserves the (broken-but-equivalent) behavior. Still must switch the store calls + resource_ref strings.

**GAP-E [LOW] — `worker_runtime.py:51 WorkerProfileDeniedError` exception class.** Asserted by tests (test_worker_runtime.py:118, test_f009:193 via `error_type`). Not in any named rename target. Dimension 6 flags it as "verify whether 全改名 includes exception classes" — confirmed it is the ONLY `WorkerProfile*`-named exception. Recommend EXCLUDE from Wave 2 (it's a worker-runtime concept, not the profile entity) unless 全改名 explicitly extends to exception classes; if renamed, the two test string assertions follow.

---

## (2) ORDERING / COUPLING HAZARDS

**HAZARD-1 [BLOCKER] — store revision methods MUST precede the authoring rewrite.** `save_agent_profile_revision` / `list_agent_profile_revisions` / `_row_to_agent_profile_revision` **do not exist** (verified: `agent_profile_revisions` is referenced ONLY in migration_117). `_publish_worker_profile_revision` (ops:825/841) and `worker_service` revisions doc (409) cannot be switched before these land. → store methods are sub-wave **2a**, before any authoring switch.

**HAZARD-2 [BLOCKER] — `agent_profile_revisions` DDL missing from `sqlite_init.py`.** Verified: the F117 ALTER block (sqlite_init.py:1240-1256) backfills the 9 *columns* but creates NO `agent_profile_revisions` table; only migration_117 creates it. Any fresh test DB (which uses sqlite_init, not migration apply) will have the table absent → `save_agent_profile_revision` fails at runtime. The DDL byte-identical add to sqlite_init is a hard prerequisite, co-located with store-method add in sub-wave **2a**.

**HAZARD-3 [BLOCKER] — read-switch BEFORE archive-sync = archived-worker-dispatchable regression.** Confirmed concrete: `worker_service.py:861 _handle_worker_profile_archive` writes status=ARCHIVED only to `worker_profiles` (no `_sync` call — verified the 3 sync callers are apply/publish/bind at 948/1031/1095, archive is not among them). ALL read consumers gate on `status==ARCHIVED` (capability_pack:413/470, session_service:784, chat — and now GAP-A/B/C readers). If any read flips to `get_agent_profile.status` before the archive write flips to `save_agent_profile`, an archived worker reads status=active (stale mirror) and stays dispatchable. **Archive write-switch and the read-switches must land in the same atomic sub-wave.**

**HAZARD-4 [BLOCKER] — the Work-column rename contradiction (the maps' #1 ambiguity, now resolved as a real break).** migration_117:524 `ALTER TABLE works RENAME COLUMN requested_worker_profile_id TO requested_agent_profile_id`. But the Work Pydantic field (control_plane/work.py:33, delegation.py:278), `work_store.py` SQL (27/51/84/359), `sqlite_init.py` works DDL (361-362/902/1130-1137), and ~60 production refs ALL still use `requested_worker_profile_id`. **If migration_117 apply lands in the Wave 2 window, `work_store` SELECT/INSERT hit a non-existent column → every Work save/load crashes.** Mitigating fact I verified: migration_117 is a **standalone manual-apply** migration, NOT in the sqlite_init auto-run chain (sqlite_init:1238 comment confirms it's separate). So the break is only triggered by explicit apply — but the maps must make this an EXPLICIT GATE: **either (a) defer the works-column rename out of migration_117's Wave-2-applied scope (split the migration), or (b) couple the full Work field + work_store SQL + sqlite_init works DDL + ~60 refs rename into Wave 2.** This cannot be left as "decision needed" — running the migration as-is corrupts work_store. Note this also touches `connection_metadata.py:13/73` TURN_SCOPED envelope keys, which are wire-contract and would need careful handling either way.

**HAZARD-5 [MEDIUM] — coordinator/agent_service `agent-profile-{worker-profile-id}` prefix removal vs AgentRuntime.worker_profile_id (Wave 4).** `_coordinator.py:987` and `agent_service.py:633` PREPEND `agent-profile-`; `_coordinator.py:1010` reverse-replaces it to derive `worker_profile_id`. Wave 2 collapses these to a single row, but `AgentRuntime.worker_profile_id` (Wave 4) must stay populated. The maps correctly flag this but it's a real ordering trap: removing the reverse-replace at 1010 must keep `worker_profile_id` derivable (set `= agent_profile_id`), or the AgentRuntime composite-key lookup (`find_active_runtime` at dispatch_service:671, entity_ensure:207) breaks.

---

## (3) DISAGREEMENTS / BEHAVIOR-BREAK RISKS BETWEEN MAPS

**DISAGREEMENT-1 — chat.py:245 inclusion is "confirm with main session" in Dim 1 but "switch all three" in Dim 4.** Dim 1 (read-path) hedges line 245 as "NOT strictly in scope... flag for inclusion." Dim 4 (store consumers) firmly includes it. **Resolution: include it.** Leaving 245 as `get_worker_profile` is a residual reader Wave 4 breaks. No real disagreement once reconciled — but the maps should not ship with this ambiguity.

**DISAGREEMENT-2 — return-type contract (WorkerProfile vs AgentProfile) for ops helpers.** Dim 2 says `_get_worker_profile_in_scope` / `_save_worker_profile_draft` return `AgentProfile`. Dim 3 (worker_service) makes its `WorkerProfile | None` annotations *conditional* on that. Dim 5 (tests) makes test edits conditional too. This is a **single decision** that 3 maps defer to each other. It must be pinned BEFORE Wave 2 coding: **helpers return AgentProfile** (cleaner, matches "the row IS the authored object"), which forces worker_service annotations (616-617, 903) and the bare `WorkerProfile` import to flip in the same commit.

**RESOLVED (not a real break) — `source_kind='worker_profile'` binding literal.** Dim 1 correctly insists this stays load-bearing. Verified no contradiction: it's the `_ResolvedWorkerBinding` identity, distinct from the `worker_profile_mirror` metadata marker. Keep both.

**RESOLVED (not a break) — `is_worker_behavior_profile` kind-vs-metadata.** I verified migration_117 DOES add+set `kind='worker'` (line 364-366, 396, 418) on apply. So post-migration the kind path works; pre-migration the metadata path works. The maps' "live instances have NO kind column" is correct ONLY for the pre-apply state. New worker-detection code must use `is_worker_behavior_profile` (dual), never bare `kind=='worker'` — maps are unanimous and correct.

---

## (4) SAFEST SUB-WAVE SEQUENCING (each stays green)

**2a — Store foundation (additive, zero behavior change, stays green standalone):**
- Add `save_agent_profile_revision` / `list_agent_profile_revisions` / `_row_to_agent_profile_revision` to agent_context_store (clone worker_* impls, swap table → `agent_profile_revisions`).
- Add `_AGENT_PROFILE_REVISIONS_DDL` + index to sqlite_init (byte-identical to migration_117:474-486).
- Rename the 2 enums (`WorkerProfileStatus`→`AgentProfileStatus`, `WorkerProfileOriginKind`→`AgentProfileOriginKind`) at definition (agent_context.py:80/86) + all 12 consumer files + test enum-member access. Collision-free (verified). StrEnum values unchanged → zero DB/wire impact.
- Rename the 5 collision-free view classes (StaticConfig/DynamicContext/ViewItem/RevisionItem/RevisionsDocument). **Defer `WorkerProfilesDocument`** — hard collision with existing `AgentProfilesDocument` (verified both exist).
- Gate decision: resolve the `WorkerProfilesDocument` / `resource_type='worker_profiles'` / resolver-key / route / `get_*_document` method-name collision (Dim 6 gotcha — coordinator duplicate-key + duplicate FastAPI route). Recommend keeping the wire strings distinct in Wave 2 (rename only internal Python symbols).

**2b — Coupled authoring write-switch + authoring-side reads (atomic):**
- worker_profile_ops: delete both mirror builders (`_build_agent_profile_from_worker_profile`, `_sync_worker_profile_agent_profile`); rewrite `_save_worker_profile_draft` to write unified `agent_profiles` + `agent_profile_revisions`, **salvaging metadata `source_kind='worker_profile_mirror'` stamping + behavior-file materialize + persona_summary/summary dual-assign**; rewrite `_publish_worker_profile_revision`; switch `_get_worker_profile_in_scope` read + **add the kind-guard** (raise NOT_FOUND if not `is_worker_behavior_profile`); switch `_generate_worker_profile_id` collision read.
- worker_service: delete the 3 `_sync` calls (948/1031/1095) + the per-profile mirror call (152); switch all authoring reads (394/409/676/906); **archive write-switch (861) → `save_agent_profile` status=ARCHIVED** (closes HAZARD-3 structurally).
- agent_service: collapse create path (615-675) to single `save_agent_profile`; **switch `_handle_update_resource_limits` (GAP-D, 368/380)**; remove `agent-profile-` prefix keeping `worker_profile_id` derivable (HAZARD-5).
- _coordinator: collapse default-main path (970-997), remove reverse-replace (1010).
- agent_context_entity_ensure: delete `_ensure_agent_profile_from_worker_profile` (942 builder).

**2c — Coupled runtime read-switch (atomic with 2b's archive write — same commit or immediately after):**
- capability_pack: `resolve_worker_binding` (410), `resolve_worker_type_for_profile` (469), drop the `1207` worker fallback.
- chat.py: all three (245/256/271) + `is_worker_behavior_profile` import.
- session_service: `_resolve_direct_session_worker_profile` (783) + owner read (513).
- **GAP-A dispatch_service:686**, **GAP-B entity_ensure:218**, **GAP-C session_projection_helpers:95/107**.

**2d — Naming / wire surface (per the Wave-2-vs-Wave-3 decision):**
- action_id `worker_profile.*`→`agent_profile.*` across action_registry (330-385), worker_service dispatch keys (77-84) + the hardcoded literal at 1001, worker_profile_ops capability literals. **Strong recommendation: defer ALL wire strings (action_id, resource_type, resource_id, route paths) to land atomically WITH Wave 3 FE** — verified NO backend pytest cross-checks the FE contract, but the running app's Agents page 404s in the gap. Wave 2 = internal Python symbols only; wire strings move in Wave 3.

**Work-field (HAZARD-4):** explicit GATE before 2a — either split migration_117 to NOT apply the works rename in Wave 2, or pull the full Work-field rename into a dedicated sub-wave. Do not start 2a until ruled.

---

## FALSE-POSITIVE confirmations (maps correct to EXCLUDE)
Verified out-of-scope: `worker_profile_mirror` / `source_worker_profile_id` metadata markers (detection, must survive); `capability_pack.get_worker_profile()` builtin (returns `WorkerCapabilityProfile`, different type); `is_worker_profile=` behavior-template selector; `pack.worker_profiles` (BundledCapabilityPack field); `AgentRuntime.worker_profile_id` (Wave 4); `worker.spawn_from_profile` / `worker.extract_profile_from_runtime` (Work verbs); persisted prefixes `worker-snapshot:` / `worker-profile-`; `WorkerProfile`/`WorkerProfileRevision` classes + tables (Wave 4).

**Files with worker_profiles store calls = exactly 10 (verified):** all 10 now have a mapped or gap-flagged switch target. No 11th hidden reader.